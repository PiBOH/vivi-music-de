# TODO — VIVI Music DE (desktop) + Android ↔ Desktop sync

Legend: `[x]` done · `[ ]` to do · `[~]` in progress

## Phase 0 — Desktop foundation (completed)
- [x] DE 1.53.18 (FIX): **the liked songs travel between the two devices, and the playlist copies a pairing made are collapsed again.** The device sync always carried a list of liked song ids but nothing read it (only the playlists of an incoming snapshot were applied, and the snapshot sent back held no liked songs), so the like state was the one part of the pairing that never moved: the liked songs now travel as *entries* — a like with the song's metadata (what lets the phone store a song it has never seen), an unlike a tombstone with its time, each with a last-write-wins timestamp so an unlike is not resurrected by the older copy, and a plain id with no time can only ever *add* a like (older peer). On the desktop the session like map became that ledger (`SongActions`: pushed on a local like/unlike, applied on a remote one, never echoing back, never firing *Auto download on like*); on the phone the entries come from the liked songs of its database plus the unlikes of the session, and an arriving like is written with the metadata the desktop sent. **Constraint:** nothing in this path calls `likeVideo` — the account stays the only authority, a snapshot can never like or unlike on YouTube Music. The playlist duplication (E1034) survived 1.53.17 because a copy could carry **no** account id (the peer's own row, before the playlist was created on YouTube Music here) or **two different** ones (both devices created the account copy of the same playlist): a playlist is now also recognised by what it holds — the same name and one song list contained in the other (`samePlaylist`) — and the copies are merged into the entry that knows its account id, songs unioned, the others tombstoned locally; the cleanup runs at startup, after every merge and the moment a playlist learns its account id (`link`). *Create on YouTube Music* no longer creates a second account copy when the account already holds that playlist (the row adopts the existing account id and only the missing songs are pushed), and the account's special lists (liked songs, saved for later) no longer travel as ordinary playlists. **Constraint:** all of it is local — nothing is ever deleted on YouTube Music, so a duplicate that really exists there is the user's to remove. Verified against the user's own store: 31 entries with 12 repeated names → 12 entries, one per playlist. [APK 6.0.6.8] the phone sends its liked songs with metadata and times, applies the desktop's likes and unlikes, matches an arriving playlist against its own rows by name and songs (adopting the account id) instead of importing a second copy, and drops a row it had imported for the desktop's mirror of the liked-songs / saved-for-later lists.
- [x] DE 1.53.17 (FIX): **the seek bar, the playlist duplication at pairing, the expressive player and the rail.** `ViviSlider` put `detectTapGestures` and `detectDragGestures` in two `pointerInput` blocks on the same node, so they fought over the pointer and a drag never registered (the thumb only moved by tapping): one handler now covers press/drag/tap and the press already moves the thumb. On top of that both seek bars dropped the scrubbed position the moment the finger was lifted, snapping the thumb back to the live position until the engine caught up — the scrubbed position now stays until the player reports it (1.5 s tolerance, 4 s safeguard). The expressive player's countdown showed `-0:00` at/past the end for a whole track and is now drawn only while something is left. Pairing duplicated every account playlist because the shared identity of a playlist is its account id while both apps generate their own local id: the account id now travels (`remoteId`), a mirrored copy is merged into the single local playlist (songs of both kept), and a startup pass collapses the copies a pairing already made — locally only, never on YouTube Music (E1034). The expressive player reopens on the tab it was left on and its autoplay button writes the real *Autoplay next track* setting instead of a local flag; the rail is toggled by the "VIVI Music" title (or the logo when collapsed) and the dedicated buttons (top bar, menu, MenuOpen) plus the no-op Forward button are gone.
- [x] DE 1.53.16 (FIX): **the seven reported defects and the pairing scan.** Lyrics draw the mobile "no text" indicator again (a hole longer than 4 s, or a timestamped line with no text, gets an animated ring row — the list is no longer one row per line, and the indicator is not gated behind "Animations": it is information, not decoration); a left/right-aligned line can no longer run off the window (the active line's 1.05 scale is anchored to the alignment edge, and `MetroLyrics` measures its layout WITH the alignment so the line push compensates the growth instead of dragging a right-aligned line further left); artwork is requested at the mobile app's quality (`adjustedThumbnailUrl` = Google CDN `wN-hN-p-l90-rj` ≥544 px, YouTube's `maxresdefault`/`hqdefault` tiers, 150 px cap only when Data saver is on — the provider's `w120-h120` crop was what made covers soft); a forced playlist sync reports live progress (playlists done/total, songs moved, current playlist, which half — rendered in both entry points, built from numbers + already-translated labels so no new key); device sync carries the whole queue (a matching current track now adopts the peer's queue in place instead of keeping its own list — that is why only the one song that happened to match ever lined up — and `sync.log` records `send:`/`recv:` with track, queue size, index and flags); the `V2` player entry is gone (it was the CLASSIC layout with a bigger artwork, one player with two names); and the font import uses the Swing chooser instead of the native dialog that answers "access is denied" on `C:\Windows\Fonts`, reporting a failed copy instead of swallowing it. [APK 6.0.6.6] the pairing QR is drawn at 220 dp, the scanner is not portrait-locked, and a scan **pairs immediately** (the QR already carries address + code, so the extra confirmation tap made a good scan look unrecognized; the address is persisted before the client is created for it).
- [x] DE 1.53.15 (FIX): **full translation audit: no key can leak another language, and the mobile gaps the new screens sat on are filled.** The "Alpha in Arabic" leak had two causes: a key with translations but no English value (step 3 of `Localization.get` prints the first map that has it) and seven keys mapped to an inline English literal instead of their Android resource (`close`, `integrations`, `player_background_blur`, `lyrics_line_spacing`, `lastfm_now_playing`, `mini_player`, `randomize_home_order`), which showed English in all 52 languages while mobile had them translated (24-41 languages recovered per key; batches 13/14 had filled those five with a copy of the English text, overriding the mapping, and those placeholders are gone — `desktop_extra_translations_80.py` covers the rest). Batches 78/79 add the lyrics style names, the romanization/translation switches, the language list, the blur and the text position/line spacing in every language (`lyrics_style_none` rewritten to say *Simple*, the mobile resource behind it says *None*, so the picker was split 26/26). **Constraint:** the generator exits when a key has translations but no English source, and a key with an Android resource must map to that resource rather than to an English literal. `scripts/audit_desktop_localization.py` (new) reports 0 missing/leaking keys and 0 wrong-script values, and lists the 1 031 English strings the mobile resources never translated (device readouts, font names, the Discord/Last.fm sentences) as the next work list. The tables are split into `LocalizationTables1..9.kt` (six languages each) because one class cannot hold every literal — completing them had broken the build with `ClassTooLargeException`.
- [x] DE 1.53.15 (FIX): **the account copy of a playlist is deleted with the playlist, and the upload action is in the playlist screen too.** Deletion propagates to YouTube Music exactly as it does on mobile — but only from the delete the user performs here, never from a tombstone that arrived over the device sync (deleting there is irreversible, so it stays an explicit local action); no session or no account copy is a no-op, and `playlists.log` records `deleted` or `NOT deleted`. The *Create on YouTube Music* button the Account screen gained in 1.53.14 is also in the playlist list header, with the same confirmation and the pending count.
- [x] DE 1.53.14 (FIX): **creating a playlist offers the account copy (like mobile), and the existing local playlists can be created on YouTube Music from the Account screen.** The name dialog carries the mobile create switch (*Sync playlist* + its mobile description; without a session it says *Not logged in to YouTube* and stays local): the playlist is created locally at once and its account copy in the background, with the account's id stored on the local playlist — so a song added to it is uploaded as it arrives and a rename is propagated (**deleting is not**: it would remove a playlist from the user's account, and it is not reversible). The new Account action *Create on YouTube Music* does the same for the playlists that already exist (mobile only offers it at creation time), asking first with the count, creating them sequentially and uploading their songs, then pulling the account's playlists in the same run; it is disabled with *Every playlist is already on YouTube Music* when there is nothing to do. **Constraint:** a playlist that lives on both sides must never be mirrored again — the pull skips one that a local playlist already points to (`SyncedPlaylist.remoteId`, or the `yt-` id of a mirrored one) and the sidebar hides its online copy, otherwise each upload returns as a duplicate local playlist. `remoteId` is a new nullable field on the shared wire model (older clients ignore it and mirror as before).
- [x] DE 1.53.13 (FIX): **the listening history and the notifications list are two screens with two strings.** They shared `history_empty`, whose value in every language was the notifications wording ("Ancora nessuna notifica"), so an empty History screen presented itself as the notification list — the reported "Cronologia opens the notifications". The notification list now owns `notification_history_empty` (the batch renamed, `fa`/`iw`/`pt-rBR` filled in) and `history_empty` says "no history yet" everywhere; the screen logs what it is made of (`history.log`: tracks/searches/sections). **Constraint:** one key, one screen — never share an empty-state string between two screens again.
- [x] DE 1.53.12 (FIX): **the Artists screen has content: the corpus page is understood, and the list is no longer a server page that can be empty.** (1) The corpus (`FEmusic_library_corpus_artists`) carries its `gridRenderer` **straight in the tab content** — a field the tab model did not have, so the response deserialized to "no content", the parser found no shelf and returned an empty page **without any error** (`browse.log` said `1 section(s), 0 item(s)`); the grid is modelled now and the library parser scans every shape a library page arrives in (tab content, top-level section list, two-column secondary contents, every tab) for the first that holds items. (2) The corpus cards are `MUSIC_PAGE_TYPE_LIBRARY_ARTIST` and `MusicTwoRowItemRenderer.isArtist` only accepted `MUSIC_PAGE_TYPE_ARTIST`, so even a parsed grid dropped every artist. (3) The corpus is genuinely empty when the account follows no artist, and mobile lists artists there because its screen reads its **database's artists table** — the desktop derives them from the account's saved songs (a few pages, one per channel id, song artwork as the picture) and caches them in `~/.vivimusic/artists.json` ([ArtistsStore]), the desktop counterpart of that table (and what is drawn offline). **Constraint:** a library page's shape must never be inferred from one fixed path — `shape=` (now in `browse.log`) is what tells "the account has nothing" apart from "the parser understood nothing". The artist page itself parses all of its section/title shapes too, and an empty page gets a message with a **retry**.
- [x] DE 1.53.11 (FIX): **the lyrics renderer is a real port again: ten distinct styles, `Alpha` as the no-effect one, and `MetroLyrics` on graphemes.** The six word styles only animated with per-word timings, which plain LRC never carries, so on most songs they all fell back to the same plain line — the styles that "do the same thing" the user reported. Each style now estimates its word timings from the line's own duration (mobile's `APPLE_V2` estimator) and keeps its own recipe; each also has its own sentence-level fallback, so no picker entry ever renders as another. `MetroLyrics` moved onto a measured text canvas with the hyphen crescendo (equal-share segments per `-`, last one springing, the group springing back out over 600 ms), the per-character nudge (`0.038·sin(π·lp)·e^(−3·lp)`) and the line push (per-character widths summed per line, the line shifted by half of it) — **constraint:** the halo is offset glyph copies because a `BlurMaskFilter` has no `DrawScope` equivalent, and mobile's two divergent spring constant sets are unified, which is what makes centring correct. A frame-rate clock (`rememberSmoothPosition`) extrapolates between the player's ~40 Hz samples and dissolves the correction, the line cross-fades (alpha/scale/blur) are animated, a static line never reads the clock, the Metro canvas reads it in its draw scope, and lyrics now ignore the "Animations" master switch while still honouring "Animation speed" (the old `LYRICS_SPEED` timeline stretch is gone: it made the words light up before the voice on `fast`). `{agent:v1|v2|v1000}` alignment is applied at last.
- [x] DE 1.53.10 (FIX): **the sidebar groups stack again, the embedded sign-in completes the session, and the transport buttons press.** 1.53.5 put the three collapsible sidebar groups inside `AnimatedVisibility`, which lays multiple children out on top of each other, so every entry of a group was painted over the same spot ("the sidebar is one squeezed row"): each group is a `Column` inside its transition now — **constraint:** `AnimatedVisibility` is not a `Column`, a group of rows must be wrapped. The reported `login-debug.log` showed the sign-in handing over a session with `missing critical: [LOGIN_INFO]`, which the server answers with 401 while the same cookies pasted by hand authenticate; YouTube issues that cookie for its own domain and the window opens on `accounts.google.com`, so the window now visits `www.youtube.com` once (7 s), returns to `music.youtube.com` for the ids and only then hands over, `hasFullSession` requires the critical set, and a still-incomplete session is handed over anyway (logged as PARTIAL) so nobody gets stuck; a 401 without `LOGIN_INFO` is tagged `E1033` instead of being reported as an expired session. The player's glass buttons also take an expressive press scale.
- [x] DE 1.53.9 (FIX): **the 1.53.6-1.53.8 strings are translated in all 50 languages.** Six of them (`lyrics_options`, the thumbnail play/pause row + description, the swipe description, `sync_in_progress`, `sync_finished`) are desktop-only and hand-written in `desktop_extra_translations_76.py`; the six that come from the mobile app (`enable_swipe_thumbnail`, `swipe_sensitivity(_desc)`, `yt_sync`, `ytm_sync`, `sync_playlist`) are mapped to the Android resources with the 7-21 languages that app leaves out filled in by the same batch. **Constraint:** `_fill()` asserts the value count against the language list, so a batch can no longer be committed with one language quietly shifted (the first draft of this batch had Malayalam missing and every later language off by one).
- [x] DE 1.53.8 (FIX): **the UI now goes idle when the music is paused.** The mini-player background layer built its glow/live-mesh loops with no condition at all — every style, playing or not — and the equalizer bars, the player background (pulse, live mesh, visualizer wobble) and the canvas backdrop's Ken Burns zoom all ran whether or not anything was playing, so the window redrew 60 fps from launch to exit. Each loop now exists only while it has something to show (the moving styles, while playing) and the paused frame is a fixed value. `CanvasBackground` also stopped re-blurring a window-sized bitmap every frame: the blur is rasterized on a fixed 384 dp layer and only scaled to cover — the rule introduced in 1.52.5 for the blurred artwork backdrop (size-dependent effects must not sit on a `fillMaxSize` node) applied to the last place that still broke it. **Evidence:** the user's logs show the audio path is healthy (`played 10020ms of 10020ms wall (100%)`, `0 device stalls`, `gc` peaks of 33 ms), so the load was the render loop, not the pipeline. **Constraint:** a new animated background must take an `animate`/`isPlaying` flag — an endless `rememberInfiniteTransition` in a always-composed layer is what keeps a Compose Desktop window busy.
- [x] DE 1.53.7 (FIX): **`Auto sync with account`: the account's playlists are mirrored locally.** `PlaylistSync` reads the signed-in account's playlist list and each playlist's songs (4 requests at a time, so a large library does not get rate-limited) and writes them into `PlaylistStore` under `yt-<playlistId>`, where they can be played, queued, reordered and edited like any local playlist. It runs on sign-in, when the option is switched on, and from the Account screen's `Sync playlist` button, which also shows the last result; each run is in `playlists.log`. **Constraint:** a mirrored playlist is written only when its name or song list changed — `updatedAt` is the device sync's last-write-wins key, and bumping it for free would let an untouched playlist beat a real edit made on the paired phone. The sidebar drops the online copy of a playlist it has already mirrored (it used to list the same playlist twice).
- [x] DE 1.53.6 (FIX): **the player carries the mobile lyrics controls, and four settings rows stopped printing their key.** A **Lyrics options** button opens the mobile lyrics menu from the player itself (`LyricsQuickMenu`: style, position, word glow, Apple Music blur under the VIVI Music style only, blur, tap-a-line-to-seek, auto-scroll, thumbnail play/pause, text size and line spacing — applied live and persisted), reachable from the expressive player's lyrics panel too; the mini players' swipe-to-change-song gesture is on the full player's artwork as well, and the new **Show play/pause on thumbnail** option makes a click on the artwork start/stop the song (the tap and the swipe live in separate `pointerInput`s — see bug #65: a `detectTapGestures` followed by `detectDragGestures` in the SAME block only runs the first one). `enable_swipe_thumbnail`, `enable_swipe_thumbnail_desc`, `swipe_sensitivity`, `swipe_sensitivity_desc` were never added to the English table, so the Appearance screen showed the raw key in every language: they are mapped to the mobile resources now (the slide/switch wording) plus one desktop sentence. **Constraint:** every new UI string goes into the generator's tables in the same commit that uses it — a key with no entry renders as the key itself.
- [x] DE 1.53.5 (FIX): **every expansion animates and follows the settings.** Sidebar groups, the bottom bar and the right Now Playing panel were drawn with a plain `if` (no transition at all), the sidebar's rail and chevrons kept a fixed spring that ignored the master switch, and the right panel had neither animation nor switch control. They now share `Animations.panelEnter/panelExit`, `barEnter/barExit` and `sectionEnter/sectionExit` (expressive spring, `Animation speed` scaled, instant when Animations is off) and opening the full player is a vertical expansion of the bar instead of another horizontal screen slide. **Constraint:** new panels must take their transitions from `Animations`.
- [x] DE 1.53.4 (FIX): **the history remembers.** Search terms are recorded when a result is *acted on* (playing/queueing/opening), not only on Enter; `HistoryStore` keeps the last 200 started tracks (with play counts) and 50 search terms in `~/.vivimusic/history.json`; the History screen lists both above the account's server history with per-list `Clear`, and a remembered term reopens in Search. `Pause listen history` now actually stops the writes, writes are debounced 1.5 s and flushed on exit, and the Home seeds survive a restart. **Constraint:** history is local to the machine — it is never pushed to the account.
- [x] DE 1.53.4 (FIX): **the localization gap.** Six content/artist keys showed the raw key in English builds (the extra batches translate languages, never English) — they map to Android now. The 42 lyrics keys had no mapping at all; 33 now use the mobile wording (2184 → 860 untranslated strings) and the 9 desktop-only sentences are translated in the 49 languages (`desktop_extra_translations_75.py`). **Note:** the residual 860 sit in the ~15 languages the mobile app does not translate those keys in; `scripts/check_localization.py` lists them per language.
- [x] DE 1.53.3 (FIX): **the Content screen is the mobile one, and every row works.** Hide explicit / hide video songs / hide YouTube Shorts / show artist description / show artist subscriber count, plus the editable lyrics provider list (switch + up/down per provider; the saved order is the order `DesktopLyrics.fetch` asks them in, a provider that is off is skipped). The filters are applied where the items enter the UI (`ContentFilters`, cached like `Animations`, reloaded on startup and on every edit) — home shelves, browse, search, album, playlist, artist, artist-items and library — never per row. The artist page also shows the description now (it was parsed and dropped). **Constraint:** an empty provider list means the built-in order with all on, so the last enabled provider cannot be switched off; and the filter wording reuses Android's translations wherever they exist. **Note:** `Hide YouTube Shorts` and `Hide video songs` rely on the flags the innertube parser sets, so they can only filter what the API marks.
- [x] DE 1.53.2 (FIX): **the Artists screen loads again.** The library page it opens (`FEmusic_library_corpus_artists`) is parsed by `YouTube.library`, not by `YouTube.browse` — browse only maps the two-row cards of a browse page, so the request succeeded and the screen stayed blank with nothing in the logs. `BrowseScreen` now routes every `FEmusic_library*` id through the library parser (same as the Library screen) and records every outcome in `browse.log` (`browse ok … → N section(s), M item(s)`, `(empty page)` when the server returned an empty page).
- [x] DE 1.53.1 (FIX): **the lyrics renderer is a style-by-style port of the mobile one, plus the desktop-only `Alpha` style.** `Simple`/`Fade`/`Glow`/`Slide`/`Karaoke`/`Apple Music` copy the mobile alphas, weights and halo strengths (mobile `Glow` is a word-level halo that grows with the square of the fill — 1.53.0 had made it a sweep across the line, which the mobile style does not do); `Apple Music V2` lays the line out word by word and reveals it character by character, estimating word timings from the character counts when the source has none; `VIVI Music` runs one global wave over the sentence with the per-word halo, the 0.75/0.50/0.30/0.20 alpha falloff, the progressive blur and the 1.05 active scale; `Lyrics V2 (Fluid)` floats each word up to 4 dp while its bright copy is revealed behind a travelling edge; `MetroLyrics` draws the per-word flat fill with the landing wobble (0–750 ms) and the mobile distance falloff; the new `Alpha` draws the pre-port plain list. All of it runs on one speed-scaled timeline, so `Animation speed` now reaches every style the same way. **Constraint:** the styles are distinguished by their own numbers, not by a colour — a branch that only recolours is what made the picker look like one effect before. **Note:** `MetroLyrics` is drawn per word on the desktop; the mobile original runs per grapheme (hyphen crescendo, per-char nudge, line push).
- [x] DE 1.53.1 (FIX): **the expressive player's lyrics follow the lyrics options.** Its `LyricsScreen` call passed the display object alone, so the nullable size/line-spacing parameters fell back to 18 sp / 1.35 and overrode what the user had set; it also always asked for synced lyrics. Both now come from the settings, like the other players.
- [x] DE 1.53.0 (minor): **the lyrics styles are styles of their own, and the settings screens are laid out like the mobile app's.** `Glow` became a *line* effect (a light sweeps the whole line over its duration while a halo breathes around it) instead of a slightly larger shadow on the per-word fill; `Apple Music V2` reveals the line *character by character*, the mobile granularity, instead of repeating the word-level `Apple Music` effect; `Slide` (tight leading edge, breathing halo, glow on the words already sung), `Karaoke` (wider seven-stop fill, halo growing with the square of the progress) and `Metro` (flat bold karaoke fill plus a distance fade) each follow their own mobile recipe; the two new styles `LYRICS_V2` (the fluid one: the sung word floats up on a sine and its bright copy is revealed behind an edge travelling across it) and `METRO_LYRICS` are in the picker, and `Metro` is the one style that animates lines with no word timings (180 ms per word, 30 ms apart). The root settings went from seven groups to the mobile app's four (account/updates, media & player, features & data, system & support) with the mobile row order inside each and the desktop-only rows beside the mobile row they belong to (`Lyrics` next to `Appearance`, `Language` next to `Content`); `Player & audio` follows the mobile grouping (audio quality → crossfade → history duration → playback → misc → desktop-only slider style and stream cache); the lyrics screen uses the mobile row order. **Constraint:** every new style must set itself apart in BOTH renderer paths (`buildWordSpans` for the text styles, `WordFlowLine` for the word layout) — a branch that only changes a colour is what made the picker look like one single effect.
- [x] DE 1.53.0 (FIX): **the lyrics options apply where the mobile app applies them.** `Animation speed` now scales every style including the two that ignored it (`Glow`'s sweep divides by it, `Apple Music V2` splits the word over a speed-stretched playhead); `Apple Music blur` is shown — and applied — only for the VIVI Music style, as on mobile, instead of dimming every other style through a switch that appeared to do nothing; `Animation speed` hides itself while the master `Animations` switch is off. The two new style labels are translated in all 52 languages (`desktop_extra_translations_70.py`). **Note:** the rest of the advanced-lyrics keys (42 of them: romanization scripts, position labels, the other style names…) are still English-only in every language — that was the approved state of the 1.51.0 port, and the dedicated translation pass is still pending on the user's go-ahead.
- [x] DE 1.52.9 (FIX #3): **the Windows audio threads now join the OS "Audio" class, and power throttling is off.** The 1.52.8 export contains the mechanism: `50ms sleep returned 1211ms late (= 1161ms held up at the writer's priority; heap 38/2048MB, gc counters frozen, cpu 5% of 57% busy)` followed by `audio device stall: … 94%` (and a 59% one next to `audio cushion low: only 11ms`), i.e. the writer — ~8 wake-ups/s, nothing else to do — was **not scheduled** while the JVM used 4-6 % CPU and allocated nothing worth collecting; the ring drained and the gap became audible. Its only cushion is the device ring, which Java Sound **hard-caps at 1000 ms on Windows** (measured: 8 s/4 s/2 s requests all granted 176400 bytes; macOS grants 4 s), so nothing above a second can be buffered away there. `AudioThreadBoost` (JNA → `avrt.dll`) makes the writer, the watchdog and the decode thread call `AvSetMmThreadCharacteristicsW("Audio")` + `AvSetMmThreadPriority(CRITICAL)` — what a browser's audio thread does, which is why the same track is smooth in Chrome — and asks the OS once for a 1 ms timer, `ABOVE_NORMAL_PRIORITY_CLASS` and EcoQoS power throttling off. Calls verified live on Windows 11; every outcome is logged (`audio scheduling: …`). **Constraint:** MMCSS is per-thread, so the boost must be called *on* the audio thread (and released when a per-track thread ends); never assume `Thread.MAX_PRIORITY` is enough on Windows.
- [ ] DE: **a Windows scheduling freeze longer than ~1 s is still audible**, because Java Sound's device ring is hard-capped at 1000 ms there (measured: any request above 1 s is granted exactly 176400 bytes; macOS grants 4 s) and the PCM queue cannot help once the writer stops feeding it — MMCSS now protects the ~1 s range, and going beyond it means leaving Java Sound for a native WASAPI render path (event-driven, with its own ring and device-notification handling) or finding a bigger ring in the current backend. Do not start this without a measurement first: the same logs have to show the writer frozen outside `out.write()` before blaming the output path.
- [x] DE 1.52.9 (FIX #3): **two measurement gaps closed.** `GcMonitor` records every collection in the session's new `gc.log` (GC notifications, no `-Xlog` flag — a packaged image cannot point one at the session folder), flags pauses over 100 ms in `playback.log` and adds `last GC pause Nms Ns ago (worst …, total …)` to the stall line, so an exported session separates "the JVM was stopped" from "the process was not scheduled" without guessing. The writer also attributes every pass: time inside `out.write()` is device backpressure (normal), time outside it with PCM already queued is the thread not being scheduled (`audio writer stalled: … — the thread was not scheduled, the output itself is fine`). **Constraint:** the export must keep every category complete — `gc` is in `AppLog.KNOWN_CATEGORIES` for that reason.
- [x] DE 1.52.8 (FIX #82): **an interrupted update download can no longer pass as complete.** The installer was written directly under its final name, so leaving the Updates screen (its `rememberCoroutineScope` is cancelled with the composable) or quitting the app mid-transfer left a truncated file that every later check accepted — "downloaded", then a broken installer. Now the bytes go to `<name>.part` and the rename is the completion marker, the release asset's size (when the API gave one) is verified before promoting and while offering an existing installer, stale `.part` files are swept, and the transfer itself runs on `UpdateState`'s own scope so a screen change does not cancel it. **Constraint:** a file's *existence* is never proof that a download finished.
- [x] DE 1.52.8 + 1.52.7: **the Fedora/RPM support and the AUR archive.** A best-effort `rpm` job builds `VIVIMusic-<version>.rpm` from the jpackage app image (see the 1.52.7 entry above), and the AUR files (PKGBUILD, SRCINFO, the `.install` hook) now ship packed in `VIVIMusic-<version>-AUR.tar.gz`: `makepkg` needs the hook next to the PKGBUILD, so they cannot be split, and a loose `.install` in the release asset list is noise (the bot filtered it, the user reported it). The guide, the site's download pages/dialog (`.rpm` + AUR) and the Telegram bot's labels are updated. **Constraint:** never publish the AUR hook as its own asset.
- [x] DE 1.52.8 (FIX #3): **pauses are not stalls.** The priority watchdog and the device check both counted paused wall time, so a resume produced "50ms sleep returned 393772ms late" and "played only 7445ms in 581733ms (1%)" — diagnostics that cannot be disproved and that pollute every exported log. The watchdog now skips while paused and drops the first post-resume sample; the device check restarts its window on resume. Evidence from the reporter's macOS logs (all sessions, 1.52.6): zero `audio stall` / `audio output starved` / "device ran dry" lines, device fed 99-100% of wall time, ~3 s cushion.
- [x] DE 1.52.7: **a native Fedora/RPM package ships with every release** (`VIVIMusic-<version>.rpm`, built from the jpackage app image by `scripts/generate_rpm_spec.py` in a new best-effort `rpm` job). RPM stores scriptlets in the package header, so the uninstall cleanup is inlined in `%postun` (`dnf remove vivi-music-de` keeps one final backup and wipes the caches, like the `.deb`/AUR/Windows uninstallers) — the `.deb` cannot do this because dpkg deletes the payload before `postrm`, which is why it needs the helper copy in `/usr/share`. The cleanup body is read from the shared `scripts/uninstall-cleanup.sh`, never copied. Layout matches the AUR PKGBUILD (`/opt/vivi-music-de`, `/usr/bin/vivi-music-de`, desktop entry, hicolor icon), metadata is `PiBOH` / `GPL-3.0-or-later` / `Applications/Multimedia`, and requirements come from `rpmbuild`'s automatic soname detection so no Fedora-only package name is hard-coded. **Constraint:** a scriptlet body is macro-expanded by RPM, so a literal `%` in the cleanup script must be escaped (the generator aborts instead).
- [x] DE 1.52.6: **`Build Android APK` reaches the publish step again.** The Kotlin/resource build was green and both flavours produced an APK (the run downloaded 36 MB gms + 35 MB foss), but the publish job failed on its own assumption: it looked for `apk/vivi-<flavor>.apk` after `download-artifact`, while `upload-artifact` chooses the artifact's internal root itself (the common ancestor of the uploaded paths) so a single-file upload need not land at the root. The job now locates the files with `find` and flattens them into `apk-flat/` before publishing, and the "missing APK" guard lives in that step — a partial build still aborts the run. **Constraint:** never depend on where `download-artifact` places a file; locate it, then normalise.
- [x] DE 1.52.5: **the lyrics options reach every style, and the two selectors are dropdowns.** The word-by-word layout (`APPLE_V2` / `VIVIMUSIC_1`, the default one) is built from separate word composables and read none of the three: raw smoothstep instead of the speed-scaled progress, every word flush left regardless of "Text position", and a fixed 0.25 em row gap instead of the line-spacing value (now derived from `lineHeight / fontSize`). The text size and line spacing sliders wrote a separate state while the renderer drew from the `LyricsDisplayOptions` copy it was handed, so the value moved and nothing happened until another option was touched. `APPLE_V2` also had no motion at all (always scale 1, no offset, no blur) and was indistinguishable from plain text: un-sung words now sit smaller and lower, the sung one fades and lifts. Style and position are `M3SettingsDropdownItem` rows like the rest of the settings. **Constraint:** a new option must reach BOTH renderer paths (`buildWordSpans` for the text styles, `WordFlowLine` for the word layout) — the word layout is the default, so a miss there is invisible in review and very visible in use.
- [x] DE 1.52.5: **the resize stutter was the blurred backdrop.** "Blur" and "Apple Music" used `fillMaxSize().blur(48.dp)`: the artwork was requested at the window size and blurred at that size, and both depend on the size, so every resize frame re-decoded and re-blurred a full-window bitmap. Now a fixed 256 dp layer is blurred once and only scaled to cover. **Constraint:** keep size-dependent effects (blur, RenderEffect, shadow) off `fillMaxSize()` nodes — rasterize at a fixed size and scale.
- [x] DE 1.52.5: **two more position-keyed app-level loops removed** (Discord presence / Last.fm, and the listening-session accounting): both were keyed on `playerState.positionMs`, so they restarted ~20x/s and the Discord loop pushed a presence update on each one. Keyed on the track now, polling the controller once a second; the macOS now-playing loop reads the controller rather than a value captured at start.
- [x] DE 1.52.5 (FIX): **the "Artists" screen came up empty.** The parser required the shuffle and radio endpoints on an artist card, and the library artist grid carries neither, so every card parsed to `null` (and a card without the optional menu crashed the page with an NPE on `menu`). Both endpoints are optional now and the menu lookup is null-safe, as in the mobile parser (#3).
- [x] DE 1.52.5 (FIX #3): two diagnostics were reporting problems that did not exist. `audio integrity` counted ~20 "sample table discontinuities" per track — the constant 1824-byte gap across a fragment boundary is the next fragment's `moof` box (a gap in bytes, not in audio) while the samples inside a fragment are contiguous by construction; only a fragment starting *inside* the previous one is a real overlap and is what gets logged now. `audio cushion low` fired at every track change, where the outgoing line's ring is empty on purpose while the PCM queue already holds the next track; it is only evaluated while that line is still being fed. **Evidence behind #3 (today's Windows logs):** 0 device stalls, 0 queue starvation, device fed 99-100% of the wall time; the only remaining lines are 8 `audio priority stall` (a 50 ms sleep returning 141-507 ms late) with process CPU at 0-9% and GC counters flat, i.e. the OS descheduling an idle process rather than our buffers running dry.
- [x] APK 6.0.6.5 (mobile-only): **the Android build compiles again** — `values-az/` held four files (the old `vivi_strings.xml` plus a batch that wrote `strings_az.xml`, `vivi_strings_az.xml`, `updater_strings_az.xml`) and two of them declared the same 67 keys, so `aapt2` aborted with `Duplicate resources` and `Build Android APK` never reached the publishing job (no APK since 6.0.6.3). Consolidated into the canonical `strings.xml` / `vivi_strings.xml` / `updater_strings.xml`: 1384 keys before and after (verified by diffing the key sets), the old value kept on every conflict (the batch had left English fragments in some values), and the desktop localization generator — which only reads those three names — sees the Azerbaijani strings now. Behind that, `SimpMusicLyricsProvider.kt` was an orphan: SimpMusic had been dropped from the lyrics providers, but the file stayed and imported the `EnableSimpMusicKey` that was deleted with it (unreferenced by the registry and the UI), so the Kotlin compilation failed as soon as the resources merged — the file is removed, and the provider is **not** restored. **Constraint:** per-locale resource files always use the canonical names, never re-add a provider that was removed on purpose, and the `vivi-music-de-apk` branch needs the same cleanup when the mobile sources move there.
- [x] DE 1.52.4: **the issue tracker is the official repository's now.** The 80 issues of the fork were transferred to `PiBOH/vivi-music-de` keeping their original order (the fork is empty now), and every reference was remapped — 338 occurrences in 18 files (`CHANGELOG.md`, `TODO.md`, `AGENTS.md`, the code comments and one workflow comment), URLs included, so `#4` is `#3`, `#5` is `#4`, … `#85` is `#81`. **Constraint:** references always carry the official number (the fork's numbering is historical and the old fork URLs survive only as redirects), and the site/bot links live in the official repository too.
- [x] DE 1.52.2: **the website moved to its own branch.** The site left the DE branch (`.websitede/` is gone) and now lives at the root of **`gh-pages`** — pages, styles, images, fonts and the two data files — with a copy of `pages-deploy.yml` on that branch so a push publishes it immediately; the default-branch copy covers `schedule` and `workflow_dispatch` (those always run from the default branch) and `Release Manifest` now commits `releases.json`/`changelog.json` **into `gh-pages`** instead of into the DE branch (and asks for a deploy explicitly, best effort, because a push made with the job token starts no workflow). **Constraint:** put site edits on `gh-pages` only (no version bump, no `v`, no issue), and keep the two `pages-deploy.yml` copies in sync.
- [x] DE 1.52.2: **the dead build workflows are gone**, with the files that only existed for them: the four legacy workflows that compiled the removed `composeApp` project (`build-linux`, `build-macos`, `build-windows`, `build-windows-custom` — and with the last one `installer/windows/ViviMusicDE.iss`, `installer/windows/README.md`, `logo.png`), plus the upstream leftovers that could never run here (`nightly`, `nightly-telegram` from a `beta` branch that does not exist — and with it `scripts/send_telegram.py` —, `build`, `build_pr`, `ci`). What is left: `auto-release`, `build-android`, `build-desktop-*`, `pages-deploy`, `release-manifest`, `stale`.
- [x] DE 1.52.2: **the release notes no longer list the website bookkeeping commit**: `Auto Release` filters `chore(website): refresh the static release manifest` out of both the commit list and the extracted changelog section (and since the manifest moved, it is not a commit of this branch at all any more). Same for the Telegram bot, which reads those notes.
- [x] DE 1.52.2: **`PiBOH/vivimusicde_bot` follows the new layout** — it posts releases from `PiBOH/vivi-music-de` (the `SOURCE_REPO` default and the workflow env no longer point at the old fork), keeps the hourly check, and builds the optional custom-APK line from the fixed `.releases/apk/latest` URLs (`vivi-gsm.apk` / `vivi-foss.apk`) since the APKs are no longer release assets.
- [ ] Split the mobile sources onto the dedicated `vivi-music-de-apk` branch (today it is still a stale full copy of this branch, and `app/` + the shared modules live here): the branch must hold the Android app with its own `settings.gradle.kts` and its own `Build Android APK` workflow, while `vivi-music-de` keeps the desktop edition and the modules it compiles against. The eleven shared modules (`innertube`, `lyricsProvider`, `spotify`, `lastfm`, `kizzy`, `shazamkit`, `jiosaavn`, `canvas`, `applecanvas`, `vivimusiccanvas`, `sync`) exist in both app branches, so a fix to one of them has to land on both.
- [x] DE 1.52.0 (minor) + APK 6.0.6.4: **`~/.vivimusic/settings.json`, the editable options file** (`SettingsFile`) — mirrors the options only (camelCase keys = the app's field names), rewritten on every change and watched while the app runs, so a hand-edited value applies immediately (`settingsFileRevision()` is the remember key of every option read from the store) and a value edited while the app was closed wins at startup. Excluded, by rule: credentials/API keys, histories, queue/library/playlists, pairing bookkeeping, window geometry and transient session state — a stray key in the file is ignored (`SettingsFile.allowed`). **Constraint:** new options must consciously be an *option* (mirrored) or *data* (excluded), and the file must keep only what a user can configure.
- [x] DE 1.52.0 + APK 6.0.6.4: **the APKs are published to `.releases/apk/latest`, never to a release.** `Auto Release (Desktop)` no longer builds, waits for or attaches an APK (the `ignore_apk_failure` input and the whole fetch-and-attach step are gone) and the upstream `Production Release Build` workflow was deleted; `Build Android APK` is manual-only (`workflow_dispatch`) and recreates the `apk-latest` branch with a single force-pushed commit holding `.releases/apk/latest/vivi-gsm.apk`, `vivi-foss.apk` and `version.json`. The desktop Devices screen links the two fixed URLs (GMS first), the website's APK buttons use them too, and the companion app's updater reads `version.json` and compares **version codes**. Verified by running the publish step against a local bare remote (single commit kept across two runs, `version.json` valid). **Constraint:** never attach an APK to a release again, and never grow the `apk-latest` history (it must stay one commit).
- [x] DE 1.52.0: every repository link moved to the official repository `PiBOH/vivi-music-de` (desktop branch `vivi-music-de`, mobile branch `vivi-music-de-apk`) — app, installer, templates, security policy, docs, site and the AUR generator; the site is served from `https://piboh.github.io/vivi-music-de/`.
- [x] DE 1.51.2 (FIX #3 — root cause): **the 30 s stutter was a hidden `System.gc()`, not the audio pipeline.** Skiko's `FrameWatcher` coroutine (`gcDelayMillis = 30000`, `minFramesToRenderer = 1000`) calls `System.gc()` whenever the window drew fewer than 1000 frames in the last 30 s — the normal state of a player that only repaints a seek bar — and an explicit gc() is a **full stop-the-world** collection. Proven on the packaged image with `-Xlog:gc`: `Pause Full (System.gc())` at 30.05 s intervals, 46-69 ms fresh and **1976 ms** in a long session, freezing the audio writer thread and the Compose snapshot loop at the same instant (exactly the reporter's "music skips + mini UI lag"). Fixed with `-XX:+ExplicitGCInvokesConcurrent` in `desktop/build.gradle.kts`: same reclamation, **zero `Pause Full`**, the 30 s event becomes a bounded 2.9-9.9 ms young pause absorbed by the 8 s PCM queue + 1 s ring. **Constraint:** do NOT "fix" this with `-XX:+DisableExplicitGC` — Skiko and JavaFX's `BaseResourcePool`/`ImagePool`/`WCRenderQueue` also call gc() as a *pressure* signal for native pools, and ignoring it would let native memory grow unbounded; the concurrent flag keeps that path working without the pause.
- [x] DE 1.51.1 (FIX #3): the playback diagnostics now cover what the exported logs could not see — the **sample table is verified for continuity** (a `moof` fragment that does not continue exactly where the previous sample ended is logged as a `sample table discontinuity`, which is what a demux-induced skip looks like) and the **sound card is sampled every 10 s** for audio actually played vs. wall time (`audio device stall` = the gap is below our buffers). Each track ends with `audio integrity: N discontinuities, M device stalls, K frames scanned`, so one log says whether the audio was ever fed discontinuous PCM. **Evidence from the reported logs:** the macOS machine shows zero starvation/cushion/stall lines and a 4 s device ring; the Windows machine showed only ~120-160 ms `audio priority stall`s — **those were later traced (1.51.2) to the 30 s explicit gc(), not to the pipeline**, so the 8 s queue + 1 s ring were never the thing to fix. **Constraint:** the macOS 4 s ring must NOT be shrunk — those same logs contain writer pauses over a second, which a smaller ring would turn into audible dropouts.
- [x] DE 1.51.0 (minor): animated lyrics ported from the mobile app — word-by-word styles (Simple / Fade / Glow / Slide / Karaoke / Apple Music / Apple Music V2 / VIVI Music), every display option (glow, blur, tap-to-seek, auto-scroll, alignment, text size and line spacing), romanization (`LyricsRomanizer`, pure-JVM port of the mobile tables: katakana incl. sokuon, Hangul with assimilation rules, seven Cyrillic orthographies with per-language detection, Devanagari and Gurmukhi with the inherent vowel) and AI lyric translation (OpenRouter/OpenAI-compatible or DeepL, cached per track/language). The shared parser now keeps the per-word timings the old local `parseLrc` threw away, and it lives in `lyricsProvider` with unit tests. **Note:** the new lyrics keys ship in English only for now (approved for this port batch; other languages fall back to English) — the translation pass is a separate step when the user asks for it.
- [x] DE 1.50.76: the site's release manifest (`Release Manifest`) also runs hourly (`cron: '0 * * * *'`, at :00 CEST) and pins `ref: vivi-music-de` on its checkout — it used to refresh only when `version.txt` changed, i.e. before the release assets existed, which is why the downloads page/dialog needed a manual refresh to show them. Safe on a schedule because the default branch is `vivi-music-de`, not the `main` mirror.
- [x] DE 1.50.76: `Auto Release (Desktop)` has a manual-dispatch input `ignore_apk_failure` (default off, dispatch only) that publishes the release even when the Android build failed or its APKs are missing; with the toggle off the APKs stay mandatory on release pushes.
- [x] DE 1.50.75 (HOTFIX): the launch crash shipped in 1.50.72-1.50.74 — `NoClassDefFoundError: androidx.compose.material.icons.Icons$Outlined` — is fixed. Excluding the extended icon artifact on the runtime classpath also removed `material-icons-core-desktop` (extended is its only route into the project) and core is what declares the `Icons` accessors; the extended jar holds just the per-icon `...Kt.class` files, which is why the earlier attempt to "keep the outer classes" in the minimized jar could not work. The exclude now drops only the extended jar and core is re-added at the version the Compose plugin resolves. Verified by loading all seven `Icons` accessor classes plus sample per-icon classes against the packaged image classpath (control: the same probe without the core jar reproduces the crash). **Constraint:** icon minimization may only ever remove the extended jar — `material-icons-core` MUST stay on the runtime classpath, and a `check` in `desktop/build.gradle.kts` now fails the packaging if it does not.
- [ ] DE: everyone on 1.50.72/1.50.73/1.50.74 has to install 1.50.75 by hand (a build that crashes on launch cannot run the in-app updater), so the fixed setup must be reachable from the site's download page and the releases page.
- [x] DE 1.50.74 (FIX): no dropout at track start — the output line was started while its ring was still empty, so the device went dry 70 ms after a track started (the reported `the device ran dry here`); it is now primed with 300 ms of audio before the device starts consuming (or started early when the producer has nothing more), the resume path no longer starts an empty ring, and the starvation line says whether anything had been handed to the device at all (#3).
- [x] DE 1.50.74: the exported log zip carries the newest 20 session folders only (`LogExporter.MAX_SESSION_FOLDERS`); older sessions stay on disk. If an archive turns out too small for a report, raise that constant — it is the single knob.
- [x] DE 1.50.73: the activity log records every setting change at its full value (values longer than 60 chars used to be shortened to 34) and the exported support zip keeps that detail, so a report can be reproduced from the logs alone; the per-file 2 MB trim stays (a file keeps its newest lines) — ask before relying on it for very long sessions.
- [x] DE 1.50.72: installers trimmed — the packaged runtime now ships only the Material icons the app references (`MinimizeIconsJarTask`: full artifact on the compile classpath, ~7.8 MB minimized jar on the runtime one) and the Inno script uses `lzma2/max` + solid compression; app image ~194 → ~134 MB, Windows `setup.exe` 156 → ~129 MB, same payload cut on `.msi`/`.dmg`/`.pkg`/`.deb`/`.AppImage`. Published formats unchanged (exe+msi, dmg+pkg, deb+AppImage+PKGBUILD) and the JavaFX WebView/ICU left intact. Constraint: icons must be referenced literally (`Icons.<Style>.<Name>` in `desktop/src`) or they are left out of the packaged jar.
- [ ] DE: the Windows setup is ~129 MB and cannot go below 100 MB while keeping the sign-in WebView — the remaining bulk is `javafx-web` (~31 MB, `jfxwebkit.dll`) + `icudtl.dat` (10 MB) + `skiko-windows-x64.dll` (12 MB) + the 78 MB jlink runtime, and the jars are already deflated so the outer compression adds little. Only levers left: subsetting the four bundled app fonts (~8.6 MB raw) and shrinking the jlink image.
- [x] DE 1.50.71 (FIX): the volume slider is heard at once — the level now goes to the sound card's own gain (`MASTER_GAIN`, verified available on the reporting machine), which the driver applies while it consumes its ring; the 1.50.68/69 write-time scaling could only affect audio not yet in the device ring, which holds a full second (#79).
- [x] DE 1.50.71 (FIX): lyrics are found for stylized-Unicode titles — names are folded to plain Unicode (NFKD + dropped marks/invisibles) for provider queries and for the KuGou title/artist match, so `ＭＩＧＵＥＬ 𝑷𝒉𝒐𝒏𝒌` is matchable; cache version bumped to v6 (`lyricsProvider/TextFolding` + unit tests) (#81).
- [x] DE 1.50.71 (FIX): playing no longer recomposes the whole app ~20x/s — the audio level is collected inside the visualizer background instead of at the root (`AWT-EventQueue-0` was ≈50% of a core of continuous painting), the level feed is decimated to ~14/s, and the synced-device loops idle while unpaired (#3).
- [x] DE 1.50.69 (FIX): volume changes are audible immediately — the gain is applied by the writer on the block it is about to hand to the device instead of at decode time (the producer renders 8 s ahead, so every change waited for the queue to play out); the applied value is logged (#79).
- [x] DE 1.50.69 (FIX): a volume drag no longer rewrites the whole settings file ~20x/s (coalesced write, 400 ms) and the session polling loops no longer re-read and re-parse it every 500-800 ms (in-memory snapshot refreshed on every write) (#79, #3).
- [x] DE 1.50.69 (FIX): the silence-cut gate and the "download N s behind" figure are measured in decoded frames — the previous version divided compressed AAC bytes by the PCM byte rate and under-reported the cushion ~10x, holding cuts back even on fully cached tracks (#3).
- [x] DE 1.50.69 (FIX): the playback-stall probe runs at the writer's own thread priority (it was MIN_PRIORITY, so it reported "frozen" for delays the audio never saw) and logs process/system CPU next to heap and GC, which tells a JVM pause from a saturated machine; in the reporting session the 8 s queue + 1 s device buffer absorbed every stall (no starvation line) (#3).
- [x] DE 1.50.70 (FIX): lyrics cache bumped to v5 — a wrong association cached before a resolver fix was served from disk forever (verified: the only cached entry on the reporting machine was `Blu Da Ba Dee` → `Move Your Body - Eiffel 65`); the version is a named constant and MUST be bumped on any provider-selection/validation change (#80).
- [x] DE 1.50.70: the video-captions source (`get_transcript`) added in 1.50.69 was removed — verified with direct requests that it answers `400 FAILED_PRECONDITION` for every video, captioned ones included, so it could never answer (the Android app's `YouTubeSubtitle` is failing silently for the same reason) (#80).
- [ ] DE: a working "exact video" timed source is still missing for tracks the community servers do not index — the captions would have to come from the player response (`captions.playerCaptionsTracklistRenderer.captionTracks[].baseUrl`), which needs a model in the shared `innertube` module (#80, open).
- [~] DE 1.50.69: the video's own timed captions were added to the lyrics chain (mobile's `YouTubeSubtitle`) — **removed in 1.50.70**, the endpoint does not answer (#80).
- [ ] DE: painting while a track plays still costs ~0.5 core on the reporting machine (measured: `AWT-EventQueue-0` ≈ 3.1 s of CPU in 6 s, Skiko WindowsOpenGLRedrawer + `dwmFlush` vsync) — the per-tick recomposition of the whole tree is gone in 1.50.71, what remains is the renderer itself (candidate: software/ANGLE backend or a lower animation rate) (#3).
- [x] DE 1.50.68 (FIX): streaming playback pre-buffers 8 s of source before the output line is opened (capped at 3 s of wall clock, skipped for a cached file), so the PCM queue has something to absorb delivery pauses with instead of running pinned to the download frontier (#3).
- [x] DE 1.50.68 (FIX): "Skip silence" only cuts when at least 3 s of source is buffered ahead — it consumes source without producing output, so cutting at the frontier was what starved the sound card; otherwise the silence plays and the hold-back is logged (#3).
- [x] DE 1.50.68 (FIX): the stall line now reports the missing source in seconds (`download 0.9s behind`) and `audio output starved` reports the unplayed cushion plus "the device ran dry here", closing the one hole the `audio cushion low` check could not see (#3).
- [x] DE 1.50.68 (FIX): the look-ahead cache pass waits while the playing track's cushion is below 20 s (bounded at 60 s), so prefetching can no longer take bandwidth from the stream being listened to (#3).
- [x] DE 1.50.67 (FIX): the Home "Your artists feed" header browses `FEmusic_library_corpus_artists` instead of the param-less library container (which the server rejects with 400), and a rejected browse gets the new E1032 code instead of being mislabelled E1031 ("sign in again") in the error box and in `browse.log` (#78).
- [x] DE 1.50.67 (FIX): the live log window no longer crashes the app — log lines are keyed by a monotonic id from the logger instead of their text (two identical lines in the same millisecond killed it), and the home chip/mix rows (whose titles can repeat) are keyed by position + title (#76).
- [x] DE 1.50.67 (FIX): the lyrics shown belong to the playing song — a KuGou candidate must match the requested song and artist names (token overlap, both field orderings, `feat.`/parenthesised suffixes ignored), not just the duration, and a synced file running >60 s past the end of the track is discarded; the module is shared with the Android app (#77).
- [x] DE 1.50.67 (FIX): resuming from pause keeps the pre-rolled PCM in the queue instead of dropping it, so playback no longer starts with the silence logged as `audio output starved` (#3).
- [x] DE 1.50.67: new `window` log category — the window's state (decorated/visible/iconified/bounds) at creation, when it is shown and on every minimize/maximize/restore, plus the Skiko render API and the screen-recording hint; always created and exported like the others.
- [ ] DE: OBS/screen recording — verified that window capture sees and captures VIVI in **both** chrome modes (`SunAwtFrame`, no `WS_EX_TOOLWINDOW`, BitBlt returns real pixels in 80% of the sampled area); waiting for the exact symptom (window missing from the list vs black preview) before automating/documenting the WGC method.
- [x] DE 1.50.66 (FIX): application callbacks leave the audio threads — position/level/buffered/duration go through a dedicated pump thread, so the seek bar, lyrics, crossfade and history can no longer delay the writer (#3).
- [x] DE 1.50.66 (FIX): the device buffer is verified, maximized (up to 4 s) and logged at every track start; PCM is handed over in ~120 ms blocks; a cushion monitor logs `audio cushion low` before the ring can run dry, and a JVM stall watchdog logs stop-the-world pauses with heap/GC counters (#3).
- [x] DE 1.50.66: JVM tuned for low pause times — 2 GB heap cap, 384 MB young-gen cap, 20 ms G1 pause target (the default 25%-of-RAM heap let G1 build far longer pauses) (#3).
- [x] DE 1.50.66: "Skip silence" reports what it cuts (per-run length + per-track total) so an intentional jump can't be mistaken for a playback gap (#3).
- [x] DE 1.50.66 (FIX): macOS "Media keys" off applies immediately (handlers removed, tile can't be re-claimed) and a paused loaded track keeps the "Now Playing" claim, with every delivered remote command logged (#63).
- [x] DE 1.50.65 (FIX): playback is gap-free — the decode thread only renders PCM into an 8 s queue and a dedicated max-priority writer thread owns the sound card, so scans/network waits/GC pauses are no longer audible; the queued tail plays out on a normal end of track, and stop/seek/failure drops it immediately (#3).
- [x] DE 1.50.65 (FIX): the output gap is diagnosable — the stall line in `playback.log` now includes the queued audio, and the writer logs `audio output starved: queue empty waiting for decode` with the line headroom when the queue runs dry (#3).
- [x] DE 1.50.65 (FIX): macOS "Now Playing" works on a fresh launch without toggling the switch — the tile is cleared with the new `viviClearNowPlaying` (session, handlers and app identity stay alive) instead of `endSession`, and pushing a track re-registers the session on its own (#63).
- [x] DE 1.50.64 (FIX): the header's audio output device button is wired to the real picker instead of being an empty click handler, and the device list is built from `Mixer.sourceLineInfo` so the default OS endpoint is listed (the strict format probe made it answer "no") (#73).
- [x] DE 1.50.64: the mouse X1/X2 back/forward behaviour is unconditional — the Appearance switch and its persisted setting were removed (#74).
- [x] DE 1.50.64: the header's leading no-op "Open menu" placeholder was replaced by the sidebar expand/collapse button (#75).
- [x] DE 1.50.63 (FIX): audio micro-pauses/skips reduced — the output line now asks for ~500 ms of audio (was ~250 ms, the only jitter headroom between the decode thread and the sound card), the fallback halves it (250/125 ms) before ever dropping to the legacy 8-16 KB sizes that made it audible, and every real underrun (wait > 40 ms while the line is nearly empty) is logged to `playback.log` with the wait and the remaining headroom (#3).
- [x] DE 1.50.63 (FIX): the macOS "Now Playing" tile + media keys survive a restart — the enabled intent is kept on the native side and re-asserted at registration, on every session start and on every new track, so a fresh launch no longer leaves a visible tile with disabled buttons (#63).
- [x] DE 1.50.63 (FIX): the raw `refresh` key in the Library empty state was in the sources but not in the string table; added, so the Retry button reads properly (#68).
- [x] DE 1.50.63: every new desktop string translated into all 52 languages (batch 69) — animation speed (4 keys), mouse back/forward (2), audio output device (2) and `refresh`, with "System default" reusing the existing Android translation; `scripts/check_localization.py` is back to 100%.
- [x] DE: the 1.50.62 work now carries its retroactive issue references (#64-#72) in the changelog entries — it had been implemented before those issues were opened.
- [x] WEBSITE: PageSpeed pass #2 — dropped the 419 KB Tailwind Play CDN runtime (the four utilities the pages actually use are emitted statically into `tpl.css`), the favicon went from the 635 KB logo to a 3.2 KB 96×96 PNG, and the 22 dead Google-Fonts preconnects (the fonts are self-hosted) were removed.
- [x] DE 1.50.62: mouse side buttons (X1/X2) navigate back/forward — AWT-level listener (Compose consumes the event before the window sees it), main-window only, toggle in Settings → Appearance.
- [x] DE 1.50.62: real audio output device picker (Java Sound mixers, persisted, system-default fallback) replacing the volume-bump placeholder button.
- [x] DE 1.50.62: "Animation speed" (Fast/Normal/Slow) global multiplier for UI transitions; like button in the full + mini player now writes the real account state (optimistic toggle + InnerTube `likeVideo`).
- [x] DE 1.50.62 (FIX): backup restore restores playlists again — each ZIP entry decoded independently + fallback to the settings' library snapshot, restored count logged.
- [x] DE 1.50.62 (FIX/PERF): library empty state has Retry; seekbar position reports every 50 ms instead of 100 ms; shared OkHttp connection pool + parallel dispatcher, 10 s connect timeout, 15 ms buffer poll; settings parsed once per screen at startup.
- [x] DE 1.50.62: regression scan of every `pointerInput` for the #61 pattern (`detectTapGestures` + drag detector in the same block) — only the already-fixed theme picker matched.
- [x] DE 1.50.61 (FIX): theme picker gives live feedback — the gradient bars ran `detectTapGestures` before `detectDragGestures` in one `pointerInput`, and since the former never returns the drag detector was dead code (the bars only moved on mouse release); a single gesture loop now tracks press + movement, a `#RRGGBB` field applies a pasted color live, and the accent-intensity slider persists on release instead of per frame (#61).
- [x] DE 1.50.61 (FIX): macOS "Now Playing" + media keys work without the Accessibility permission — the session is now always registered (OS-level integration, no permission involved) and the "Media keys" switch only toggles its remote commands (off clears the tile); the Accessibility hint/button is gone, the helper now sets `MPNowPlayingInfoCenter.playbackState` (what makes macOS treat the app as its Now Playing source) and no longer publishes the app name as the track title, and the whole lifecycle is logged to `playback.log` (#63).
- [x] DE 1.50.61: macOS window chrome follows the app's Light/Dark mode (`viviSetWindowAppearance` sets `NSAppearanceNameDarkAqua`/`Aqua` on `NSApp` + every window, re-applied on theme change) instead of always being light (#62).
- [x] DE 1.50.60 (FIX): Windows uninstall fixed for real — the details box of the uninstall progress page can only be written to while the page exists, so both the box and the user-data cleanup now run inside `usUninstall` (creating it in `usPostUninstall`/`InitializeUninstallProgressForm` raised `Control 'TNewMemo' has no parent window`, which killed the uninstaller before it cleaned anything); every write is guarded and falls back to `%TEMP%\VIVIMusic-uninstall.log`. Reproduced and verified locally with a real install/uninstall (#48).
- [x] DE 1.50.59 (FIX): floating "Now Playing" widget can no longer leave the screen — the saved position is clamped to the union of every attached display on restore and after each drag (off-screen drop snaps back), so it can never be lost again (#60).
- [x] DE 1.50.59 (FIX): Windows uninstall no longer aborts with `Control 'TNewMemo' has no parent window` — the uninstall details box was created before the progress page was displayed (no window handle), leaving an orphaned control that killed the uninstaller while freeing controls; it is now created in `CurUninstallStepChanged(usUninstall)` (#48).
- [x] WEBSITE 1.50.59: motion forced on every page and every device — scroll reveals auto-tagged on all pages (threshold 0 + stagger + invisible-block fallback), ambient gradient drift in the background, tap feedback where hover used to be, body fade-in, no `prefers-reduced-motion` opt-out.
- [x] DE 1.50.58 (PATCH): the Windows uninstaller's closing message now lists exactly the two paths that survive (newest `.vivide.backup` + `device-sync.json`) instead of claiming fonts/playlists were copied next to them (#10).
- [x] DE 1.50.57 (PATCH): uninstall keeps only the newest `.vivide.backup` (restore point: settings + playlists) and `~/.vivimusic/device-sync.json` on every OS — no more `backups\uninstall-<ts>\` folder; all older backups, playlists.json, fonts, updates/audio/video/canvas/lyrics caches, logs and artwork are deleted. Windows picks the newest backup by the `YYYYMMDD_HHMMSS` suffix (auto_backup_* vs vivimusic-de_*), shell hooks use `ls -1t`; verified by running the shared script against a fake home. INSTALL-GUIDE + website updated (#10).
- [x] DE 1.50.56 (FIX): floating "Now Playing" widget no longer crashes the app on launch — the aligned default position (`WindowPosition(Alignment.TopEnd)`) has unspecified x/y (NaN), and rounding that NaN threw (`Cannot round NaN value`) ~0.5 s after the widget appeared. Unspecified positions are ignored now (#59).
- [x] DE 1.50.56 (FIX): Windows uninstall no longer aborts with 'Type Mismatch' — `GetDateTimeString('yyyymmdd_hhnnss', '', '')` passed an empty string for its `Char` separators (compiles, fails at runtime); now `#0`, plus the whole cleanup is wrapped in `try/except` so the uninstall always completes (#48).
- [x] DE 1.50.55 (FIX): Persian (fa) and Hebrew (iw) tables completed — the last 2 languages with keys falling back to English (592 keys, batch 68). `scripts/check_localization.py` now reports 100% coverage: every language table contains every English key (all 52 + English).
- [x] Convert the 7 network modules (`innertube`, `spotify`, `lastfm`, `kizzy`, `shazamkit`, `lyricsProvider`, `jiosaavn`) from `com.android.library` to `kotlin("jvm")`.
- [x] `desktop` module (Compose Multiplatform) with a search PoC via `innertube`.
- [x] Native icons: Windows `.ico`, macOS `.icns`, Linux `.png`, all regenerated from the official `desktop/icons/logo_vmde_official.jpg` source.
- [x] CI workflows: per-OS builds (Windows/Linux/macOS) + auto-release to GitHub Releases.

## Phase 1 — Sync: pairing + settings (completed)
- [x] Shared `sync` module (data model + OkHttp WebSocket client).
- [x] `sync-server/` relay (Node.js): 6-digit pairing + mailbox for offline devices.
- [x] Android `DeviceSyncManager` (Hilt): push/pull of the shared settings subset.
- [x] Desktop pairing UI + JSON settings store (`~/.vivimusic/`).
- [x] Cloud relay: `wss://vivimusic-device-sync.onrender.com` is set as the default URL on both mobile and desktop. **DEPLOYED AND VERIFIED ON PRODUCTION (2026-09-02)**: deployed via the `render.yaml` Blueprint on Render (service `vivimusic-device-sync`, Oregon, free tier); `/health` → `ok`, pairing + bidirectional snapshot sync + PING/PONG clock sync verified against the live server. Fallback documented: Hugging Face Spaces via `sync-server/Dockerfile`.
- [x] Pairing screen in the Android app (Settings).
- [x] Offline LAN (same Wi-Fi) pairing via a local desktop WebSocket relay (desktop hosts, Android connects by setting the relay URL to `ws://<lan-ip>:<port>`).
- [x] Mobile LAN discovery: NSD/mDNS "Find desktop" + QR scan in the Devices screen.
- [x] QR pairing auto-fill: the desktop QR now encodes the relay address + the current 6-digit pairing code (`vivimusic://pair?addr=…&code=…`); the mobile scanner parses it and pre-fills both fields so the user only verifies the code and taps Pair; scanning a QR while already paired un-pairs first so the new code starts clean.
- [x] Unpair on close: both relays detect a socket close, wait a 15s grace period for a reconnect, then clear the pair + notify the still-connected peer — so closing either app un-pairs both, while a transient network blip no longer breaks a healthy pairing (only the live socket triggers the unpair). (Cloud mode requires a redeploy of `sync-server`; LAN works out of the box.)

## Phase 2 — Sync: queue + playback position
- [x] Capture the queue and position from the Android player (`pushPlayback` is wired from MusicService on track/play changes).
- [x] Resume on desktop: apply `pendingPlayback`; the desktop also pushes its playback and applies incoming snapshots (bidirectional), with echo suppression.
- [x] Precise sync: seek is pushed instantly (both sides), same-track commands apply as a lightweight in-place seek (no restart), and positions carry a timestamp (`positionAtMs`) so receivers extrapolate while playing; PING/PONG clock-sync removes clock skew between phone and PC.
- [x] Drift auto-correction: while playing, the position is re-pushed every 5s (`SyncServer.RESYNC_TICK_MS`) so players re-align continuously; a 1 s tolerance skips near-no-op seeks to avoid audio glitches. Explicit user seeks are flagged (`PlaybackSnapshot.userSeek`) and applied exactly on the peer, while drift-ticks only catch up FORWARD — the leader is never dragged back by the follower's stale position (fixed the visible seekbar "jump back").
- [x] Resolving/buffering handshake: `PlaybackSnapshot.isResolving` is set by the desktop while it resolves/downloads (until the first position report = audio actually flowing) and by the mobile while ExoPlayer is `STATE_BUFFERING`.
- [x] Position-sync jump-back fixed: the relay clock offset was measured only 25 s after connect (slow EMA convergence), so right after pairing the devices extrapolated from raw local clocks and kept seeking each other by the clock skew. The first PING now fires immediately on connect, the first PONG sets the offset directly (no EMA), and `positionAtMs` is only stamped once the offset is known.
- [x] Volume sync (two channels): the in-app player volume (`volume`) mirrors the mobile/desktop slider pixel-for-pixel, and the native OS system volume mirrors Android STREAM_MUSIC <-> desktop OS volume (WASAPI master on Windows, `pactl`/`amixer` on Linux, `osascript` on macOS — best-effort). The in-app channel can be disabled independently with the "Sync VIVI volume" toggle; both sides poll with echo guards + retry on a dropped push, and the toggle itself is part of the shared settings snapshot. DE 1.35.3: wired the EXPRESSIVE player's volume slider (was a no-op placeholder) and made `startCurrent` restart finished tracks from 0:00. DE 1.35.2: a local volume drag wins for 2 s over incoming remote values.
- [x] Paired device names shown on both sides (desktop announces its real hostname, mobile shows the paired desktop name and vice versa).
- [x] Play/pause button stuck on "play" while audio kept playing (DE 1.33.112): the player position callback re-applied the initial `resumeWhenReady` intent as `isPlaying` on every ~100 ms report, overwriting a later manual play/pause toggle. The intent now applies only on the resolving→ready transition; later reports only advance the position.

## Phase 3 — Sync: library
- [x] Sync liked songs, albums, artists and playlists at the transport level: the mobile app observes its library and pushes a `LibrarySnapshot`; the desktop receives, persists and exposes it (and pushes its own). UI-side apply waits for the desktop local-library store (Phase 5).

## Phase 4 — Desktop audio playback

- [x] UI density scale (100/85/75/65/55%) applied app-wide via a density override (Settings → Appearance → Density & grid) + custom adaptive grid item size (small/medium/large/xlarge) for album/artist/playlist grids.
- [x] Screen transitions between navigations (Off/Fade/Slide) via `AnimatedContent` around the main screen switch (Settings → Appearance → Screen transitions). Translated in all 47 languages.
- [x] Slider styles (Slim / Squiggly / Wavy) via a custom `ViviSlider` (Settings → Player & audio), applied to the seek bar and the volume slider. Translated in all 47 languages.

- [x] **AUDIO IS STABLE AND FROZEN (Golden rule):** desktop audio output works (last verified DE 1.33.63). Do NOT modify audio playback code (`AudioPlayer`, `StreamResolver`, `SystemVolume`, the desktop `PlayerController` play/resolve flow, or the mobile `MusicService` playback core) unless the user explicitly asks. What works must not be touched. (DE 1.33.77+ added a configurable in-memory cache of resolved stream URLs — TTL slider 1–60 min, default 10, plus a "Forever" step; DE 1.33.107 added look-ahead prefetch: while a track is loaded the next 3 tracks are resolved + downloaded in the background and their lyrics fetched into a persistent `LyricsCache`.)
- [x] JVM audio backend: pure-Java `jaad` AAC decoder + Java Sound (play/pause/resume, position). The seek slider takes its duration from the player response (`videoDetails.lengthSeconds`) and reports it immediately (the queue `NowPlaying` also carries the known duration), the reported position is clamped to the track length, and the periodic re-sync tick only pushes when the position actually advanced. The decoded AAC sample count is only a fallback when `lengthSeconds` is missing; seeking preserves the pause state; stale/truncated cached audio files are detected and re-downloaded; position reports are throttled to ~10 fps.
- [x] Port stream resolution: NewPipe + the multi-client innerTube fallback chain, seek, and a multi-URL retry (tries every candidate and retries without `Range` on 403) are done.
 DE 1.35.7: FASTER START — the resolver now returns the NewPipe URL immediately when the extractor succeeds, instead of also running the whole ~12-client chain + HEAD validation as insurance on every first play (~0.7-1.4 s saved); the client chain only runs when NewPipe is bot-blocked and now collects 2 playable candidates without a HEAD per candidate (the download itself is the validation, the player falls through to the next candidate on failure); `MIN_START_BYTES` lowered 64 KB -> 32 KB so the decoder starts on the first fragment instead of waiting for a second one (~0.2-0.4 s saved).
- [x] Load feedback: the full player and mini-player show "Resolving audio…" / "Downloading…" (with a spinner) while the stream is resolved and downloaded.
- [x] First-play reliability: pressing Play on a track whose stream is not loaded yet (e.g. restored from the persistent queue at startup, or a previous load failed) now triggers a real resolution + load via `startCurrent()` instead of a no-op `resume()`, so the track starts on the first press.
- [x] APK 6.4.30 (mobile stream retry): on a 403/IO stream error the retry now rotates the guest identity (`BotDetectionMitigator.rotateGuestSession`, guest sessions only) before re-resolving, so a bot-flagged/expired googlevideo URL isn't returned verbatim again — fixes tracks failing to start / auto-skipping / pausing in the first seconds even when unpaired. (User-requested; the previous `YTPlayerUtils.forceRefreshForVideo` was a no-op.)
- [x] APK 6.4.31: playback errors now show a visible on-screen toast with the exact error code (`IO_UNSPECIFIED (2000)`, etc.) plus the message, de-duplicated per (track, code) so the retry loop doesn't spam; the same error is also written to `PlaybackLogManager` (Settings → Content → Playback logs).
- [x] APK 6.4.32: refactored the mobile stream-retry path — shared `reResolveCurrentTrack` helper (removed the duplicated rotate/seek/prepare block) and deleted the no-op `YTPlayerUtils.forceRefreshForVideo` stub.
- [x] APK 6.4.33: mobile stream resolution now prefers a NewPipe-signed URL (matched to the selected audio format) over the shared ANDROID_VR client URL — mirrors the desktop's NewPipe-first resolver and avoids the CDN bot-flagging behind `IO_UNSPECIFIED (2000)`. The client URL stays as fallback.
- [x] APK 6.4.34: mobile stream downloads now send a browser-like `User-Agent` (Firefox, same as the URL validation and the desktop's NewPipe download) instead of the default media3 UA which the googlevideo CDN was resetting; the playback-error toast now also shows the nested cause (real reason) instead of just the `Source error` wrapper.
- [x] APK 6.4.35: when every innerTube client is bot-flagged (`Video unavailable`), the mobile now falls back to a NewPipe-resolved stream URL (NewPipe resolves its own signature independently, like the desktop's NewPipe-first resolver).
- [x] APK 6.4.36: added `IOS_MUSIC` and `ANDROID_MUSIC` to the mobile fallback client chain (right after the main VR client), so music-only / YouTube-Music-signed tracks that the generic clients report as `Video non disponibile` now resolve — matching the desktop, which already plays them reliably.
- [x] APK 6.4.37: added `VISIONOS` and `ANDROID_NO_SDK` to the mobile fallback client chain, completing parity with the desktop's proven resolver for the remaining music-only tracks.

## Phase 5 — Desktop persistence + authentication
- [x] APK 6.4.44: startup freeze after in-place updates fixed — the Coil image-loader factory read DataStore with an unbounded `runBlocking` on the main thread (now bounded with a 1.5 s timeout + default), `ViviPrefCache` died permanently on the first DataStore error leaving every synchronous settings read on the slow fallback path (now retries), and the persisted queue/automix/player-state files were deserialized on the main thread (now on IO). Previously only recoverable by uninstall/reinstall.
- [x] Persistence: the desktop uses a JSON file store (`DesktopSettings` under `~/.vivimusic/device-sync.json`); Room is Android-only and stays there.
- [x] YouTube login: cookie-based login with `DATASYNC_ID`/`VISITOR_DATA` fallback remains available. The DE now exposes a cross-platform JavaFX embedded Google sign-in window with direct sign-in instructions, automatic cookie capture, persistent session storage, and a system-browser fallback when JavaFX/WebView cannot start.
- [x] JavaFX packaged runtime: replaced the Swing-dependent `JFXPanel` with a direct JavaFX `Stage`, so the embedded WebView is included in the application image without `jdk.swing.interop`; browser fallback remains available if JavaFX itself cannot start. Verified `createDistributable` and confirmed the JavaFX runtime jars are packaged (DE 1.33.122).
- [x] JavaFX startup stability: initialize the embedded WebView toolkit once via `Platform.startup` instead of the single-use `Application.launch`, preventing false unavailability/repeated fallback after the first attempt (DE 1.33.123).
- [x] Embedded WebView packaging fixed for real: package the platform `javafx-media` jar (WebView's missing runtime dependency) and include `jdk.jsobject` in the jlink image; verified with a packaged-app smoke test that creates a WebView successfully (DE 1.33.124).
- [~] Embedded login WebView still reported blank-white after opening in the packaged app: added explicit WebView dimensions, a forced re-layout/paint nudge on page RUNNING and SUCCEEDED, and `[login-webview]` console logging of load state/size/title so the remaining cause is diagnosable (DE 1.33.125). Packaged smoke test (exact layout + AWT pre-init) renders Google's sign-in page fine, so the failure is specific to the running app context — needs a user test of `.125` and the console log to pinpoint.
- [x] Removed the redundant "Accesso" heading from the login screen so the two sign-in options (Accedi con Google / manual cookie) appear directly (DE 1.33.126).
- [x] Completed ALL missing desktop translations across every language: 3,582 strings added (batches `desktop_extra_translations_24..31`); all 333 used keys exist in the English table (no more raw keys in the UI); every one of the 47 language tables now contains all 437 keys with no English fallback. Also fixed the generator to deep-merge extra batches per key and added `scripts/check_localization.py` as a permanent verification tool (DE 1.33.127).
- [x] Removed the intermediate "Log in" screen for the account flow: Settings → Account now embeds the sign-in options (Google / manual) directly when not signed in, extracted into a reusable `LoginContent` composable (DE 1.33.128).
- [~] Embedded login WebView blank-white: after the packaged smoke test proved the engine renders, forced JavaFX software rendering (`prism.order=sw`) before toolkit start to avoid the GPU-pipeline conflict with the Compose window (DE 1.33.128). ROOT CAUSE FOUND and FIXED in DE 1.34.2: the jlink runtime image was missing `java.net.http` (page load hung at RUNNING forever) and `jdk.unsupported` (`sun.misc.Unsafe` for the Marlin 2D engine) because JavaFX ships as classpath jars and jlink cannot see its module requirements. Verified with a limited-modules reproduction: with both modules the smoke test reports `state=SUCCEEDED` and the page renders (needs a user test of the new installer).
- [x] Library sidebar entry (when not signed in) now embeds the sign-in options (Google / manual) directly instead of a "Log in" button opening a separate screen — same single-screen behavior as Settings → Account, so every login entry point is consistent (DE 1.33.129).
- [x] Ported the NewUI_desktop.zip line onto the current codebase (DE 1.34.0): Apple-style single-column player, MiniPlayer suite (3 designs × 5 animated backgrounds + pure black + fullscreen), Spotify 3-panel layout with top nav header and right Now-Playing panel, player personalization settings, EXPRESSIVE capsule slider style; all repo features re-integrated (embedded sign-in, inline account/Library login, AI-translation disclaimer, installer cleanup, official toast logo, 441-key localization table). Version numbering switched to proper SemVer: minor bump for features, patch for fixes.
- [x] Sidebar collapse now compresses to a compact icon rail (72dp, centered icons) instead of hiding the sidebar entirely, in both Spotify and classic layouts; expand/collapse buttons in the rail, the classic header and the Spotify top bar; state persisted (DE 1.34.2).
- [x] Windows window-management overhaul (DE 1.34.3): removed the forced Compose Maximized/Fullscreen placement (root cause of the oversized window on non-100% DPI — enlarged UI, off-screen title bar, app unreachable, taskbar covered).
- [x] Window controls (minimize / maximize / close) are now ALWAYS visible (DE 1.35.6): the window is undecorated, but the buttons only lived in the Spotify top header, which disappears on the full player screen and in the non-Spotify layout. Extracted into a shared `WindowControls` composable; a floating top-right overlay (clicks pass through) shows them on the player screen and in the non-Spotify layout.
- [x] Fixed the giant-UI freeze when selecting a player design (Settings → Riproduzione e audio → design del player): `PlayerDesignScreen` and `DensityScreen` used `fillMaxSize()` inside the scrollable `SettingsSubScreen` → measured with infinite max height → layout sized to Infinity (enlarged UI, unresponsive, Task Manager needed). Both now use `fillMaxWidth()` (DE 1.34.4).
- [x] Embedded Google sign-in "unknown error" after the window closed: the session was captured too early (only SAPISID seen, remaining redirect cookies still arriving), so the innertube account_menu validation answered as guest (NPE).
- [x] DE-only Telegram channel update: the new channel t.me/vivimusicde is used ONLY in the DE app (About screen) and the DE website; the Android app (About + Welcome) keeps the original t.me/vivimusicapp and the issue template keeps its old invite link (reverted in 6.4.41_DE-1.34.7).
- [x] Full player felt stuck at full screen: the Classic/New/V2 designs had no back button (only Expressive did), and the sidebar/top header are hidden on the player screen.
- [x] Google sign-in validation failure after the embedded window closes ("Login validation failed"): root cause was the cookie CAPTURE, not validation.
- [x] DE 1.49.9 (PATCH): Settings → Data saver switch now updates live — the value was read once from the settings file (non-observable), so the toggle only reflected the change after leaving/re-entering the screen; now held in local observable state.
- [x] DE 1.49.8 (PATCH): volume sync now FULLY gated by the "Sync VIVI volume" toggle (the OS system-volume channel used to sync unconditionally — send/apply/poll gated on both DE and APK); tray-menu toggle applies live; Home section songs play as a queue starting at the tapped track; macOS/Linux physical media keys via JNativeHook (Accessibility permission needed on macOS; Windows hook unchanged); audio output buffer doubled to reduce glitches (esp. macOS); log export moved from Developer options to System; all remaining raw UI strings localized (Home greeting, "Your Artists Feed", "Made For You", "See all", ~15 tooltips) — 18 new keys × 47 languages (582 → 600 keys); mini-player card fixed artist-artist bug.
- [x] Sign-in still failed for some users even with a complete cookie header: `DATASYNC_ID` and `VISITOR_DATA` were treated as OPTIONAL but innertube puts them in the API context (visitorData + onBehalfOfUser) and the account validation answers as guest without them. DE 1.49.7: both are now mandatory — the embedded window reads them from the loaded page's ytcfg, the manual method fetches them from the music.youtube.com shell (www fallback), and a missing id fails fast with readable **E1030** (ERRORS.md) instead of a cryptic NPE/5xx. Labels switched from "optional" to "required" in all 47 languages.
- [x] Translate the 4 new UI strings (`close`, `listen_together_title`, `search_hint`, `up_next`) into all 47 languages — batch `desktop_extra_translations_32`, check_localization reports every language table complete with all 441 keys (DE 1.34.1).
- [x] Full desktop settings layer (same keys as the Android app).

## Phase 6 — Full desktop UI
- [x] Screens: Home, Search, Album, Artist, Playlist, Library, Player, Lyrics, Settings (Library is a placeholder until Phase 5 login; Player/Lyrics are UI-only until Phase 4 playback).
- [x] Apple Music–style mini-player, animated canvas and a full Material 3 Player (seek, volume, shuffle, repeat) are done.

## Phase 7 — End-to-end encryption
- [ ] Per-pair key exchanged during pairing.
- [ ] Snapshots encrypted before sending (the relay no longer reads the data).

## Phase 8 — Pixel-perfect UI port (mobile → desktop)
The desktop should look exactly like the Android app, except:
- the bottom navigation bar becomes a collapsible/expandable sidebar;
- the Devices (device sync) screen keeps its desktop-specific layout.

- [x] Foundation: seed-based Material 3 palette (materialKolor TonalSpot) matching the mobile theme, and a collapsible/expandable sidebar with Material icons + labels (persisted).
- [x] Home screen port: filter chips, mobile-style section headers (label + bold primary title, Play all, chevron), songs-only sections rendered as horizontal song lists (Quick picks style), mixed sections as card carousels, and a Mood & genres section (buttons navigate to a generic Browse screen). QR code always rendered on a white background.
- [x] Search screen port (search bar, live suggestions, filter chips All/Songs/Videos/Albums/Artists/Playlists, result rows/grids).
- [x] Library screen port (Material 3 filter-chip tabs, songs list + adaptive grids, Shuffle all).
- [x] Album / Artist / Playlist detail screens port (header artwork, play/shuffle buttons, track lists, related-item carousels).
- [x] Player + mini-player + queue + lyrics (large responsive artwork, animated canvas background, seek/volume, shuffle/repeat, drag-to-reorder queue, synced lyrics highlighting with configurable text size).
- [x] Settings sub-screens port (Appearance, Player & audio, Account, Content, Lyrics, Privacy, Storage, Updates, About, Notifications), keeping the custom Devices screen — with real functionality (pure black, audio quality, remember shuffle/repeat, persistent queue, lyrics text size, content language/region, update-notification placement in-app vs native). DE 1.33.102: added a Settings search box (icon next to the "Settings" title expands into a text field that live-filters the entries by title/subtitle).
- [x] Appearance sub-menu fully ported into three dedicated sub-screens: Theme (4-mode selector System/Light/Dark/Pure black + full 21-color accent palette + live preview card), App font (5 bundled fonts — System/Google Sans/Sans Flex/Outfit/Plus Jakarta Sans — with a live typography preview) and Canvas (enable toggle + source Auto/Apple Music/ViViMusic/Tidal, wired into the player's animated background via `CanvasResolver` + `CanvasSource`). The `.ttf` fonts are bundled under `desktop/src/main/resources/fonts/` and all new strings reuse the Android translations (47 languages); the three sub-screens no longer nest their own `verticalScroll` inside the settings scaffold (that caused the "infinity maximum height constraints" crash). DE 1.33.96: the selected font is now applied globally to the whole app theme (previously only the picker preview changed).
- [x] History / Changelog / Login screens (history from YouTube, live changelog fetched from the repo, cookie-based login with DATASYNC_ID/VISITOR_DATA fallback).
- [x] About screen: the GitHub repository icon is now a vector (ported from the mobile drawable) so it tints with the accent color and adapts to dark/light mode.
- [x] Backup & restore sub-menu: exports/imports a full ZIP backup (settings + playlists + account/login + library via `BackupManager`) through a native save/open dialog; backups use a single `.vivide.backup` file and legacy single-JSON `.backup` files are still importable. Import preserves the device id + first-launch date, drops any stale pairing, and prompts a restart to apply; manual backups carry the date + timestamp in their filename.
- [x] "Restart now" after a backup restore truly relaunches: `restartApplication()` releases the single-instance lock, spawns a new instance (jpackage launcher via `jpackage.app-path`, or `java -cp … MainKt` in dev), then exits.
- [x] Automatic backups (ported from mobile): optional weekly backup (checked on startup + hourly, 5-backup retention) and optional "backup before update" that runs before opening the installer.
- [x] [APK] Mobile backup restore is safe and crash-free: the archive is decompressed on a background thread into `filesDir/pending_restore`, then the process exits; `App.onCreate()` swaps the settings + database in before Room/DataStore are opened on the next launch. This never closes the shared Room database while the app is running (the old approach crashed with "database is closed") and the WAL/SHM sidecars are deleted before swapping `song.db` so the restored DB is never mixed with stale journal frames.
- [x] [APK] Restore now validates the backup's database before applying it (SQLite header magic + `PRAGMA integrity_check` + schema-version guard): a corrupt or incompatible backup fails with a clear "backup is corrupt" message instead of being swapped in and crashing the app on the next launch.
- [x] [APK] Restore confirmation: picking a backup now shows a dialog with the file name, backup date (parsed from the filename timestamp, falling back to the file's last-modified time) and the app version it was created from (parsed from the filename), before anything is applied.
- [x] [APK] Restore picker now accepts any file (`*/*`), so legacy `.backup` files from the original 6.0.5 app (no registered MIME) always appear and are selectable.
- [x] [APK] Restore swap is no longer silent on failure: the DB target dir is created when missing, and a failed staged copy keeps the backup + logs the error for retry on next launch (instead of deleting the backup without applying it).
- [~] Song / album / artist / playlist context menus port. The full song context menu (like / library / add-to-playlist / share) is done, plus a dedicated "Add to playlist" button on every song row (and on the Player + Queue screens); album / artist / playlist context menus are still pending.
- [x] Local playlist system port: create/rename/delete playlists, add/remove/reorder songs (drag-to-reorder in the detail screen), a "Playlists" screen + per-playlist detail, and cross-device playlist sync with last-write-wins (the library snapshot now carries the full playlist name + ordered songs + edit timestamp, plus deletion tombstones). Delete confirmation is deferred until after the dialog dismisses, fixing a "layouts are not part of the same hierarchy" crash; last-write-wins preserves the peer's edit timestamp on apply, so desktop create/rename/delete all propagate to the phone.
- [x] Desktop localization: the playlist and song-menu strings (rename / delete playlist / confirmation / empty / not-found / song count / like / library / share, …) are fully translated across all 47 supported languages (desktop-only keys added via a `TRANSLATIONS` table in the generator).
- [x] Desktop localization: apostrophes render correctly everywhere — the generator now decodes Android's `\'`/`\n`/`\t`/`\"`/`\\` resource escapes into real characters before re-encoding them as Kotlin literals (previously `\'` leaked through as a literal backslash-apostrophe).
- [x] Desktop localization: **100% coverage** — every desktop-only key (including developer options and backup/restore) is now translated in all 47 supported languages, with no English fallbacks left.
- [x] Desktop localization quality: filled the last desktop-only keys that were still falling back to English (device sync, updates, player basics) and overrode wrong-context / too-long Android mappings (short "Check for updates" button, "Error" ≠ "unknown error", "Playlists" ≠ "featured playlists", CPU/GPU kept short) via `scripts/desktop_extra_translations_8.py` and `_9.py`.
- [x] Mobile Devices translations: the whole "Devices" section (Devices, Device sync, Relay server, Find desktop, Scan QR code, 6-digit code, Pair/Unpair, paired state, Sync VIVI volume + descriptions) was untranslated and fell back to English — now filled in all 54 mobile locales via `scripts/add_device_sync_translations.py` (DE 1.33.111 / APK 6.4.39).
- [x] Desktop Device-sync how-to: a Material 3 "How to connect your phone" card (4 steps, LAN server recommended) on the Device sync screen, translated in all 47 desktop languages (`desktop_extra_translations_21.py`).
- [x] Global text selection removed because it made every dropdown/dialog crash with "layouts are not part of the same hierarchy" (popups inherit the `SelectionContainer` registrar — Compose CMP-2326).

## Phase 9 — In-app updater + developer options (completed)
- [x] In-app update checker: checks on startup, on entering the Updates screen, and periodically (configurable interval: manual / 6h / 12h / 24h / 3 days / 7 days); channel-aware pre-release toggle (`includePreReleases`, default on for non-stable channels). The interval dropdown dismisses the popup before applying the change, fixing a "layouts are not part of the same hierarchy" crash.
- [x] Robust asset selection: scans releases newest→oldest and picks the first that actually ships an installer for the host OS; falls back to a clear "open release page" message instead of a fake Download that opens the browser.
- [x] Debian-first installer selection: on Debian/Ubuntu (detected via `/etc/os-release` `ID`/`ID_LIKE` containing `debian` or `ubuntu`) the updater prefers the `.deb`; other Linux distros keep the AppImage first.
- [x] In-app download (progress + speed), "Close Vivi and open installer" closes the app after launching, and a "Delete installers" option to clear downloaded update files.
- [x] Update notification ↔ Updates screen sync: a single `UpdateState` (progress + downloaded installer + count) is shared by both surfaces, so acting in one is reflected in the other (and vice versa).
- [x] Selectable update source (fork vs original): desktop (default fork `PiBOH/vivi-music-de`) and mobile (default original `vivizzz007/vivi-music`) let the user choose which GitHub repo the update checks, download and notification URLs read from; threaded through `UpdateSource`/`UpdateChecker` and the Android updater + `UpdateNotificationHelper`. Mobile now parses the fork's combined tag (`<mobile>_DE-<de>-<channel>` → mobile part via `mobileVersionFromTag`) and accepts any `.apk` asset, so the fork source actually detects updates.
- [x] Queue sync is last-write-wins (like playlists): `PlaybackSnapshot.queueUpdatedAt` (shared relay-time frame) is stamped when the local queue/index changes and adopted when a remote queue is applied; on receive each side only replaces its queue when the remote edit is newer (`0` = unknown → apply unconditionally for older peers).
- [x] Repeat mode + shuffle sync in real time (both ways): `PlaybackSnapshot` carries `repeatMode` ("OFF"/"ALL"/"ONE") and `isShuffle`; the desktop pushes them via its `PlaybackSyncKey` and applies them on receive, and the phone pushes them from `onRepeatModeChanged`/`onShuffleModeEnabledChanged` and applies them in `applyRemotePlayback`.
- [x] Notifications cover **all** app notifications (update available, device paired/unpaired, developer options unlocked), each routed through the chosen mode (main window vs native); native system notifications are marked "experimental".
- [x] Notification history: every notification (in-app and native) is recorded (capped, persisted in `~/.vivimusic/device-sync.json`) and viewable from Settings → Notifications → Notification history, with a "Save notification history" toggle and "Clear history"; the generic in-app notification auto-dismisses after a user-set time (3/5/10/15/30s, default 5s).
- [x] Native notifications landing in the Windows notification center/history: `java.awt.SystemTray` uses legacy `Shell_NotifyIcon` balloons, which Windows 10/11 does **not** surface in the Action Center.
- [x] Crash/error dialog "Copy error": a global uncaught-exception handler (`installGlobalErrorDialog`, installed at startup) replaces the default AWT "Error" dialog with one that shows the full stack trace and offers "Copy error" (copies to clipboard) plus "OK".
- [x] Single-instance guard: launching while another instance is running (or starting) exits immediately, keeping the first instance.
- [x] Developer options: Settings entry always visible but disabled by default; unlockable via 7 taps on the version code or the dedicated toggle; unlock notification pointing to Settings → Developer options.
- [x] Performance overlay with two profiles — Full (all metrics) and Performance (CPU + RAM + GPU) — movable by dragging (default on), plus an option to show CPU/RAM in the window title bar and a "Title bar only" display mode.
- [x] Developer options screen fully redesigned from scratch (DE 1.33.95): prominent enable card, live monitor card with real-time CPU/RAM/GPU/network/threads/uptime tiles, display mode + profile as radio groups, and overlay/title-bar switches in a card. Functionality unchanged.
- [x] Animated intro splash: plays once at startup (click to skip), toggled in Settings → System → Intro (default on) (DE 1.33.92, GIF→JPEG in 1.33.94).

- [x] Official DE branding: replaced the desktop logo everywhere it is displayed or packaged (intro, native notifications/toasts, app resources, Windows Inno Setup/jpackage, Linux AppImage/DEB, macOS bundle, website and README) using `desktop/icons/logo_vmde_official.jpg`; platform-specific `.png`, `.ico`, `.icns` and installer bitmap variants were regenerated.
- [x] Native Windows toast branding cache: detailed toast notifications now always refresh the bundled circular logo through a versioned `logo_vmde_official.png` cache path instead of reusing the legacy old-logo file (DE 1.33.119).
- [x] Round logo shape: the official logo is clipped to a circle (anti-aliased) in every asset — app icons `.png`/`.ico`/`.icns`, intro splash, tray icon, native notifications, installer bitmaps, README and website; `generate_desktop_icons.py` re-applies the mask automatically (DE 1.33.116).
- [x] Update-found changelog: when the Updates screen finds a new version it shows a "What's new" card with the pending release's changelog (live from the repo CHANGELOG.md, parsed with `ChangelogLoader`), Material 3 card, translated in all 47 languages (DE 1.33.117).

## Phase 10 — Remaining gaps to reach 100% UI parity (mobile → desktop)
Only allowed difference: the bottom navigation bar becomes the collapsible/expandable sidebar. Everything below is still missing or divergent.

### Screens / sections missing entirely
- [x] Listen Together (mobile main tab) + its chat and "from topbar" variants (DE 1.33.88: JSON WebSocket client + create/join room, connected users, join approvals and chat).
- [x] Stats screen — removed as a duplicate of VIVI Wrapped; the sidebar entry now links straight to Settings → VIVI Wrapped (DE 1.33.91).
- [x] New Release albums screen (DE 1.33.88).
- [x] Charts screen (DE 1.33.88).
- [x] Wrapped screen: Settings → VIVI Wrapped sub-screen with the session stats card + "Show on Home" toggle (default off) (DE 1.33.80).
- [x] Song recognition (Shazam) + recognition history (DE 1.33.89: mic capture → 16 kHz resample → fingerprint → `shazamkit`, with persisted history).
- [x] Commit screen (`settings/commits`) — Settings → Updates → Commits lists the selected source branch's recent commits (DE 1.33.87).
- [x] Artist sub-tabs: Songs / Albums / Items (DE 1.33.89: TabRow on the artist page; Items uses `YouTube.artistItems`).
- [x] Keyboard shortcuts: Ctrl+Z / Ctrl+Y undo/redo navigation and Backspace/Alt+Left back (DE 1.33.89). DE 1.33.105: moved the global handler from the preview (tunnel) key phase to the bubble phase so a focused text field consumes Backspace / Ctrl+Z / Ctrl+Y first — no more navigating away while editing a search field.
- [x] Back navigation fix: no duplicate stack entries (navigating to the screen already on top is ignored) and sidebar roots keep Home at the base so back from Settings reaches Home instead of bouncing/sticking (DE 1.33.93).
- [x] Auto-playlist detail screens (Liked / Top / etc.) — `AutoPlaylistScreen` loads a library browse-id as its own pushed screen (DE 1.33.88).
- [x] Dedicated Mood & genres screen (DE 1.33.88).
- [x] Translations for the whole Phase 10 port filled across all 47 languages (Listen Together, recognition, Stats/Charts/New releases/Mood & genres, comments, username, Undo/Redo/Items) (DE 1.33.90).

### Settings sub-screens missing
- [x] Settings redesign — the whole Settings tree now follows the mobile Material 3 look: a shared card-based settings component (`M3SettingsGroup`/`M3SettingsItem`) styles the main Settings screen (grouped into General / Appearance / Player & audio / Account / Content / Privacy / About sections, search still working) and the sub-screens (Appearance, Player & audio, Notifications, Lyrics, System, Data saver) use the same card rows for navigation/toggle entries (DE 1.43.0).
- [x] **[CRITICAL FIX] Appearance sub-screens restored (DE 1.49.1)** — the five sub-screens that were wrongly flattened into inline dropdowns in 1.44.0 (Font, Canvas, Density & grid, Screen transitions, Player design) are back as M3 sub-screens reachable from Settings → Appearance and Player & audio, each using the anchored inline dropdowns; removed the duplicate legacy `miniPlayerStyle` setting that made the mini-player design reset on launch; added the missing 47-language `canvas` label.
- [x] **[CRITICAL FIX] Invisible dropdown rows (DE 1.49.3)** — root cause of the "missing options / empty sub-screens" reports since 1.44.0: `M3SettingsDropdownItem` constructed its row but never rendered it, so every dropdown-based single-choice option (Font, Canvas source, Density, Transitions, Player design, Mini-player, Slider style, Notification mode/duration, Intro style/background) was invisible and unclickable. The row is now composed via `M3SettingsItemRow`; verified live on the installed 1.49.2 (Windows).
- [x] **Font sub-screen rich layout restored (DE 1.49.4)** — back to the pre-1.44.0 design: big themed typography preview card (primary-container, "Anteprima carattere" + quote rendered in the selected font) + each font as its own radio row rendered in that typeface with its description; verified live on Windows.
- [x] **"Player design" row in Settings → Player & audio wired (DE 1.49.5)** — the entry existed but its navigation callback defaulted to a no-op, so clicking it did nothing (the identical entry under Appearance worked).
- [x] **macOS native notifications via osascript (DE 1.49.6)** — SystemTray balloons never show on macOS/Apple Silicon; native notifications now use `osascript display notification` (Notification Center), tray balloon kept as fallback.
- [x] **Login server error gets its own code E1029 (DE 1.49.6)** — a YouTube Music HTTP 5xx during login validation is now tagged E1029 (LOGIN_SERVER_ERROR) in the error message and documented in ERRORS.md with the "wait and retry" guidance.
- [x] **Developer options: export logs as .zip (DE 1.49.6)** — button in Settings → Developer options packages `~/.vivimusic/*.log` + generated system-info.txt + redacted settings-summary.txt into a STORED zip via a native save dialog; device-sync.json (contains the session cookie) is never included.
- [x] **Native title bar compatibility hint (DE 1.49.6)** — Appearance → Native system title bar shows a recommended-if-compatibility-issues hint line; translated in all 47 languages (extra translations file 55).
- [x] ERRORS.md — error-code reference (playback codes 1000–6008 + VIVI codes E1000–E1012), linked from the website footer; the fake "Now playing" card and the LIVE badge were removed from the site hero (DE 1.43.0).
- [x] Listen Together settings (hub entry) — the Listen Together screen is reachable from the sidebar (DE 1.33.88) and now hosts the full room controls (DE 1.50.0).
- [x] AI Lyrics Translation (`settings/ai`) — full provider/API-key/base-URL/model/target-language/mode/DeepL-formality screen (DE 1.42.0).
- [x] Data saver (`settings/datasaver`) — master toggle that backs up and forces off canvas + rotating artwork, restores on disable (DE 1.42.0).
- [ ] Romanization (`settings/content/romanization`).
- [ ] JioSaavn (`settings/player/jio`).
- [x] Equalizer (`settings/equalizer` + dialog) — AutoEQ `.txt` profile import, active-profile radio, delete; the DSP (biquad PK/LSC/HSC + preamp) is applied to the decoded PCM just before the output write as a pure add-on (null default = byte-identical pass-through, frozen playback core untouched) (DE 1.42.0). DE 1.46.0: added a one-click **Example: V-Shape** profile (7 bands) and a **live editor** — selecting a profile shows draggable sliders for preamp and per-band gain/Q applied to playback in real time. DE 1.49.2: each band header also shows its frequency range label (Sub-bass / Bass / Low mid / Mid / High mid / Treble) in all 47 languages.
- [x] Appearance → Animations master switch — when off, screen transitions and the intro splash switch instantly (no animations); the Screen transitions picker hides while disabled (DE 1.47.0).
- [x] Custom accent color picker — HSV gradient bars (hue/saturation/brightness) with live preview + hex, "Add to palette" saves the color as a persistent palette swatch (removable via a small ×); 6 new palette colors (Magenta, Turquoise, Coral, Lavender, Gold, Navy) with 47-language tooltips (DE 1.45.0).
- [x] First-launch screen restyled as an M3 welcome/onboarding — bundled logo, welcome title/description, language selection as Material 3 option cards with a checkmark and a Continue button (replaces the old plain language list) (DE 1.49.0).
- [ ] Spotify import (`settings/spotify`).
- [x] Integrations hub (Discord Rich Presence + Last.fm scrobbling) — Settings → Integrations (DE 1.33.76).
- [ ] Discord login.
- [ ] Notification permission (`settings/update/notification_permission`).

### Already tracked, still open (see Phase 8)
- [~] Album / Artist / Playlist context menus (the song context menu is done).
- [ ] Gradient header on Album / Artist / Playlist (currently a simple row).

### Fine-grained parity (not previously tracked)
- [x] Dynamic theme (Material You `dynamicTheme`): OS accent detection (Windows DWM / macOS / GNOME) for the Dynamic swatch (DE 1.33.70).
- [x] Player design variants (`useNewPlayerDesign`, `usePlayerV2`, `useExpressiveAlbumDesign`, `useAppleMiniPlayer`). Made visually distinct: Classic (two-column), New (single-column hero + pill play), V2 (two-column, title overlaid), Expressive (single-column, largest art, title overlaid). (DE 1.33.83)
- [x] Player background styles (gradient / blur / glow animated / apple music / live mesh). (DE 1.33.67)
- [x] Slider styles (squiggly / wavy / thin). (DE 1.33.66)
- [x] Screen transitions (fade/slide, matching `NavigationBuilder`). (DE 1.33.65)
- [~] Animated thumbnails: rotating done (DE 1.33.67); swipe / canvas thumbnail still missing.
- [x] Mini-player: swipe-to-expand, outline, pure-black mini (DE 1.33.68). Floating nav-bar interaction: the DE uses the sidebar instead, mini player already sits above it.
- [x] Mini-player / player design persistence fixed (DE 1.37.3): `MiniPlayerDesign.from()` always returned APPLE and reset the saved design at every launch; it now honors the saved key (legacy aliases included) and falls back to CLASSIC only for unknown keys. `MiniPlayerBackgroundStyle.from()` falls back to FOLLOW_THEME (was BLUR) for unknown keys; the full player design was already persisted correctly.
- [x] Song swipe gestures (swipe to play / remove) in the Queue screen (DE 1.33.69).
- [~] Advanced lyrics: line spacing + thumbnail play/pause done (DE 1.33.75); swipe lyrics / romanization / AI translation are touch/API-dependent and not portable to desktop — noted as out of scope.
- [x] UI density (density scale 55%..200%, incl. 110/120/125/130/140/150/180/200 above 100%) + custom adaptive grid size (DE 1.33.82, Settings → Appearance → Density & grid).
- [x] Sort chips for library items: A-Z / Z-A + by artist on songs (DE 1.33.71).
- [x] Home: "Quick Picks vs Last Listen" toggle, "Randomize home order", Wrapped card (session stats) (DE 1.33.72).
- [x] Integrations: Discord RPC (Windows IPC pipe) + Last.fm scrobbling/now-playing (DE 1.33.76).
- [x] Search/listen history + pause-history privacy toggles (DE 1.33.73): local recent searches on the Search screen, Privacy sub-screen with pause toggles + clear.
- [x] Settings popup (quick settings shortcut) via the sidebar Tune button (DE 1.33.74) — **removed again in DE 1.33.79** at the user's request ("procura solo fastidio").

## Phase 11 — Spotify-style UX redesign (planned, DE 1.38.x)
Opt-in style: extends the existing `spotifyLayout` toggle. When ON → Spotify palette + geometry (flat dark `#121212`/`#181818`/`#282828`, green accent `#1DB954`, 8dp corners, bolder typography); when OFF → current Material 3 tonal UI stays untouched (no regression). Sync (theme/accent with mobile) is unaffected. Each phase is verified with the toggle ON (looks right + interactive) and OFF (M3 unchanged) before moving on.
- [x] Fase 0 — Theme foundations (DE 1.38.0): flat Spotify palette in `Theme.kt` (`spotifyScheme` — dark `#121212`/`#181818`/`#282828`/`#B3B3B3`/`#1DB954`, light `#FFFFFF`/`#F6F6F6`/`#191414`), uniform 8dp `Shapes`, bolder title/label typography; the `spotifyLayout` flag (default on) now drives theme + 3-panel layout together — when off, the tonal Material 3 theme is untouched. DE 1.38.1: the Spotify primary follows the selected accent (contrast-aware on-color) instead of fixed green, and a "Spotify" swatch (`#1DB954`) was added to the palette; the Expressive player's side panel is now opaque. DE 1.38.2: the standalone Queue screen is fully accent-free. DE 1.40.0: adjustable accent intensity (saturation) slider (0-100%) applied before the accent seeds either palette. DE 1.41.0: custom-font import (native file dialog, .ttf/.otf, copied to ~/.vivimusic/fonts/, persistent) and the Italian font-preview text changed to "Dove la parola fallisce la musica stupisce.".
- [x] Fase 1 — Shell (DE 1.39.0): Spotify sidebar (pure black in dark / white in light, grey `surfaceContainerHighest` selected pill + bold full-contrast text, `surfaceContainerHigh` hover, 8dp radius; the accent-filled selection stays only in the classic layout), thin 1dp top border on the bottom player bar (cover+title/artist left, transport center, volume right — already Spotify-shaped), and the right Now-Playing panel now uses the theme panel color (was hardcoded `#121212`, broken in light mode).
- [ ] Fase 2 — Cards & grids: album/playlist/artist cards (`#181818` bg, `#282828` hover + circular green play button overlay, 8dp radius, bold title + grey artist below); Home/Browse/Library grids use these cards.
- [ ] Fase 3 — Main screens: Home/Browse/Library (chip filters, dark header gradient), Album/Playlist detail (large cover + dark green gradient header), Artist, Search, Queue, fullscreen Player adapted to the Spotify language.
- [ ] Fase 4 — Polish: hover animations (150-200ms), dark scrollbars, uniform transitions, coherent light mode (`#FFFFFF`/`#F6F6F6`).
- [ ] Out of scope (legal/technical): Spotify logo/trademarks/assets, Circular font (use an available equivalent), any change to sync/audio/notifications (presentation only).

## Desktop-exclusive features (Cider-inspired, DE 1.35.0)
- [x] Audio visualizer: `Visualizer` player background reacts to the real decoded PCM level (`AudioPlayer.onLevel` → `PlayerController.audioLevel`), smooth attack + per-bar phase wobble, dark scrim (DE 1.35.0).
- [x] Floating "Now Playing" widget: always-on-top draggable window (artwork + title/artist + Previous/Play-Pause/Next + close), position persisted (debounced), toggle in Settings → Desktop features (DE 1.35.0).
- [x] Global media keys (Windows): Play/Pause/Next/Prev/Stop intercepted even without focus via a JNA `WH_KEYBOARD_LL` hook + native message pump on a daemon thread; Windows-only, toggle in Settings → Desktop features (DE 1.35.0).
- [x] Tray menu: right-click the tray icon → Play/Pause, Next, Previous, Open VIVI Music, Quit (localized labels; tooltip shows the current track); toggle in Settings → Desktop features (DE 1.35.0).
- [x] Fullscreen lyrics (Cider-style focus mode): blurred artwork backdrop + centered synced lyrics + bottom transport bar; button in the player (Classic/New/V2), Esc/back exits (DE 1.35.0).
- [ ] Windows media flyout (SMTC: track in the Win+volume popup + flyout buttons): needs WinRT COM interop (`ISystemMediaTransportControlsInterop::GetForWindow` + hwnd) that cannot be validated without a Windows machine — deferred, do NOT ship blind.
- [x] Apple/Cider-style visual polish (DE 1.35.1): now-playing indicator in every song list (accent highlight + 3 equalizer bars animated from the real decoded audio level, idle pulse when paused); artwork ambience (colored glow derived from the blurred artwork + soft specular reflection under the art); glass transport controls (semi-transparent circles with sheen/border, accent play button); ~300 ms crossfade on track change (background/artwork/controls); Expressive side panel switched from a hardcoded dark color to the Material 3 theme surface + border.

## Infra / release
- [x] GitHub Pages website for VIVI Music DE: the static site (index + Downloads/Changelog/Sync/About subpages, dark minimal style matching the original site, logo bundled) lives at the root of the **`gh-pages`** branch and is deployed by the `pages-deploy.yml` copy that lives there via `actions/deploy-pages` on push (the default-branch copy handles `schedule` / `workflow_dispatch`). Download links and the changelog resolve the latest GitHub release client-side through the API, so the site stays current without rebuilding on every release. URL: `https://piboh.github.io/vivi-music-de/`. The site is fully responsive (hamburger nav on mobile, stacking download rows), links the Android companion back to the upstream VIVI Music site, and credits VIVIDH P ASHOKAN as the original mobile author.
- [x] Inno Setup "Start VIVI Music DE" after install: removed the redundant `launchafterinstall` task that gated the `[Run]` entry, so the final-page checkbox now actually launches `{app}\VIVIMusic.exe`.
- [x] Mobile backup files use the `.vividroid.backup` extension (manual + automatic) while the desktop uses `.vivide.backup`, so the two editions are distinguishable; older `.backup` files remain listed and importable (the `endsWith(".backup")` listing filters and the filename timestamp regex still match).
- [x] Debug APK build fix: `sync_vivi_volume_desc` used a bare apostrophe that aapt2 rejected as "Invalid unicode escape sequence"; escaped it (`\'`) so `assembleUniversalGmsDebug` (and the CI "Build Debug APK" job) completes again.
- [ ] `WINDOWS_SIGNING_CERT` + `WINDOWS_SIGNING_PASSWORD` secrets to sign the Windows installer.
- [x] Bump the version in `version.txt` on every release, advanced automatically per change.
- [x] `version.txt` reorganized into a self-documenting six-line layout (mobile version / mobile version code / mobile channel / DE version / DE version code / DE channel) with `#` comments below each field; `desktop/build.gradle.kts`, `AppInfo.kt` and the release/build workflows updated to the new positions.
- [x] Include the Android APK in the same GitHub Release as the desktop (debug APK fetched from the existing CI run).
- [x] Linux `.deb` installs on Debian too: jpackage auto-detects deps with `dpkg -S` on ubuntu-latest, which returns Ubuntu's t64-renamed names (`libasound2t64`, `libglib2.0-0t64`, …) that don't exist on Debian Bookworm.
- [x] Persistent Android debug keystore: CI (`build.yml`) restores the debug keystore from the `DEBUG_KEYSTORE` secret (base64) so the debug APK keeps a stable signature across runs (no more uninstall/reinstall on the phone).
- [x] Keep screen on while paired: the Android app sets `FLAG_KEEP_SCREEN_ON` and the desktop keeps the display/system awake (`SetThreadExecutionState` on Windows, `caffeinate` on macOS) while paired, so the OS sleeping the screen can't suspend the network and unpair the two devices.
- [x] Settings persistence is atomic: `DesktopSettings` exposes a synchronized `update(transform)` (read-modify-write) and every call site now uses it instead of `save(load().copy(…))`, so a UI-thread setting change.
- [x] DE 1.37.1: paired "must press play twice" fixed via 3s grace window after a local play/navigation command (peer pre-action paused echo ignored); manual "next" at queue end wraps to the first track.
- [x] DE 1.37.2: native title bar toggle no longer crashes (window chrome frozen at startup; setUndecorated is never called on a displayed frame); closing while maximized persists the floating restore bounds instead of the full-screen ones, so the window no longer reopens stretched over the Windows taskbar.
- [x] DE 1.41.2: hover tooltips on every button (helper composable `Tooltip` in `Tooltips.kt`, `@OptIn(ExperimentalFoundationApi::class)`): sidebar toggle, top-bar controls, window controls, mini/full player controls, search, notifications, "See all", now-playing widget, playlist rename/delete, song menu, add-to-playlist. AGENTS.md rule: all new buttons must be wrapped in `Tooltip(...)`.
- [x] DE 1.41.3: tooltips on accent color swatches (Theme & colors) with localized color names (batch 38, 22 `accent_*` keys in 47 languages); `AccentColor` gained a `key` slug field for the lookup.
- [x] DE 1.41.4: right Now Playing panel toggle in Settings → Appearance (switch, persisted per machine); `right_panel` + `right_panel_desc` in 47 languages (batch 39).
- [x] DE 1.41.5: distinct icons per action — right panel uses `VerticalSplit`, playlists (sidebar + search filter) use `PlaylistPlay`, `QueueMusic` reserved for the queue only (matches mobile).
- [x] DE 1.41.6: tooltips render above the button (ComponentRect TopCenter + 8dp) instead of on the cursor, so they never block clicks.
- [x] DE 1.41.7: right Now Playing panel auto-hides while the full player is open (any design) and restores on exit without touching the saved pref; shuffle/repeat added to the Expressive player and New/Apple mini players.
- [x] DE 1.41.8: smart tooltip placement — below-right of the pointer, flips above when no room below, clamps to window edges, never covers the click target.
- [x] DE 1.41.9: tooltip position fixed — cursor position is component-local, so the component offset (anchorBounds.left/top) is now added to place the tooltip in window coordinates (was: every tooltip at top-left).
- [x] DE 1.41.10: full localization audit — the 12 desktop-only keys (`open_vivi`, `quit`, `desktop_features(_desc)`, `lyrics_focus`, `now_playing_widget(_desc)`, `media_keys(_desc)`, `tray_menu(_desc)`, `windows_only`) were translated in all languages but never registered in the generator mapping, so every locale showed the raw key name in the tray menu / desktop features screen / lyrics focus button. Wired into MAPPING, regenerated; check_localization now reports only the dynamic `accent_${entry.key}` (false positive — all `accent_*` keys exist).
- [x] DE 1.41.11: expressive theme colors now derive from the Material accent instead of fixed brand hex values — "Made For You" mix cards (hue rotations of `colorScheme.primary`), player live-mesh background (`rotateHue(accent, 120f/240f)`), artist placeholder gradients (deterministic hue rotation per artist name), Expressive player bottom toolbar (surfaceContainerHigh + primaryContainer active tab). New `rotateHue(color, degrees)` helper in Theme.kt reusing the existing HSL logic. Splash intro keeps its deliberate dark base (Spotify/Apple style) with accent glows.
- [x] DE 1.41.12: AppImage/Linux crash with `UnsatisfiedLinkError: OpenGLApi.glFlush()` fixed — `configureRenderApi()` in main() probes for a loadable libGL (glvnd + distro paths) before the first Skia layer and falls back to `skiko.renderApi=SOFTWARE` when OpenGL is unavailable (common on Arch/minimal desktops with the AppImage). Explicit `skiko.renderApi` JVM property or `SKIKO_RENDER_API` env var wins. No native AUR asset needed — AUR builds from source; the fix is in the app itself.
- [x] DE 1.41.13: WebView login `401 UNAUTHENTICATED` on account_menu fixed — modern Google logins expose `__Secure-3PAPISID`/`__Secure-1PAPISID` instead of legacy `SAPISID`, so the API sent the cookie but no `Authorization: SAPISIDHASH` header. The hash now uses the first available of SAPISID/__Secure-3PAPISID/__Secure-1PAPISID (InnerTube.ytClient), LoginManager accepts all three, and LoginWebView.capture() keeps the most specific domain per cookie name so the hash uses the .youtube.com session cookie, not the .google.com one.
- [x] DE 1.41.14: Linux GL crash self-heal + AUR packaging assets. The libGL startup probe alone couldn't catch broken GL stacks (loadable libGL ≠ working GLX/EGL context), so the global error handler (ErrorDialog.kt) now detects the Skiko OpenGL crash, writes `~/.vivimusic/.gl-software`, and relaunches once with `SKIKO_RENDER_API=SOFTWARE`; `configureRenderApi()` (Main.kt) reads the marker on later launches and starts straight in software rendering. New scripts/generate_aur_pkgbuild.py generates PKGBUILD + SRCINFO (pinned to the release commit, real sha256) attached to releases, so Arch users can `makepkg -si` instead of fighting the AppImage.
- [x] DE 1.41.15: INSTALL-GUIDE.md at repo root (Windows .exe/.msi, Linux .deb/AppImage/PKGBUILD, macOS .dmg/.pkg, first-launch sign-in, updating, data locations, troubleshooting) attached to every release by auto-release.yml; interactive install-guide.html on the website (OS tabs + copy buttons on commands, same content as the .md) linked from the nav of all pages.
- [x] DE 1.50.2 / APK 6.4.42: relay URL in Settings → Devices was stored through a screen-scoped coroutine, so a cleared/changed value got cancelled on exit and the last-used URL came back (APK).
- [x] DE 1.50.3 / APK 6.4.43: volume sync split back into two independent channels — the "Sync VIVI volume" toggle gates only the in-app VIVI volume; the native OS system-volume sync keeps working whenever paired (it had been over-gated behind the same toggle). DE Devices screen: connection-method dropdown (Server relay recommended / Local LAN server) at the top with method-aware how-to steps, pairing QR generated for both methods, Connect button shows "Connecting…" + a note that it can take a few seconds (up to 2 min if the relay server needs to wake), and the mobile-download button renamed to "Download the adapted VIVI Music for Android (APK)" (all translated in 47 languages).
- [x] DE 1.50.4 (HOTFIX, Mac-reported): Mood & genres Home/dedicated lists deduplicated (duplicate `browseId+title` keys crashed Compose with "key was already used" on scroll); macOS Accessibility prompt spam fixed — JNativeHook registration is now gated behind `AXIsProcessTrusted()` (never attempted while untrusted, activates the moment it is granted; the Desktop features row shows an honest switch + "Open System Settings…"); the expressive player's "Nothing playing" state gained the collapse button (clearing the queue no longer traps the user in the full player); relay pairing code is auto-generated on connect via a single "Connect & Generate Pair Code" / "Regenerate Pair Code" button under the relay field; audio decode thread at max priority + visualizer level decimated to ~20 Hz to soften macOS micro pauses/skips. 4 new keys × 47 languages.
- [x] DE 1.50.6 (PATCH): settings entries/rows compacted ~15% — row padding 20/16→17/14 dp, icon tiles 40→34 dp (glyphs 24→20 dp), group gap 4→3 dp, chevron 24→20 dp, sub-screen margin 16→14 dp (shared M3Settings components, so every settings sub-menu inherits it).
- [x] DE 1.50.7 / APK 6.4.45 (FIX): bidirectional language sync with (deviceId, per-device seq) markers — the peer only applies a language that is a newer manual change made on the other side, so echoes and the phone's stale pair-time push can no longer flip the DE back to English; the newest manual change wins on either device. Radio/Charts never silently blank: charts parser walks every section-list variant, auto-issues the continuation once on an empty shell, and the UI gained a localized empty state + Retry (+ one auto retry). Density sub-screen has a single selector. Every in-app error is selectable (shared ErrorBox). DATASYNC_ID sanitized (strip trailing `||…` junk) so the account_menu validation passes. Player-design dropdowns highlight the selected option; `Localization.get` can no longer surface a raw snake_case key. 2 new keys × 47 languages (retry, charts_empty).
- [x] DE 1.50.5 (PATCH): precise seeking — jump straight to the AAC frame of the clicked time instead of decoding/discarding every frame from the start (the old path re-scanned the seek bar from zero and could only land once the download reached the target); fully cached tracks now play straight from disk; look-ahead prefetch extended to 3 next + 3 previous first, then the whole rest of the queue one download at a time; in-app VIVI volume persisted and restored at startup (no more reset to 100%).
- [x] DE 1.50.8 (PATCH): classic mini player seeking fixed — the seek slider now commits once on drag end (like the full player) instead of restarting the whole decode thread on every drag tick, so scrubbing forward/backward no longer feels dead or fights the live position; startup prefetch now downloads the current track FIRST (a restored persistent queue had nothing cached for the current track, so play had to resolve+download before the first sound — now it starts instantly from disk).
- [x] DE 1.50.9 (PATCH): About screen redesigned with a new CONTRIBUTORS section under the developer card. Contributors are loaded dynamically from `contributorsde.json` (repo root, bundled as a classpath resource): each row shows the GitHub avatar (auto-fetched from github.com/<user>.png via Coil, initials disc while loading/offline), username, a neutral-English blurb and opens the GitHub profile on click. File carries an inline `_guide` so a new contributor can be added by editing JSON only. Section header localized in all 47 languages.
- [x] DE 1.50.10 (PATCH): contributors are now read from `~/.vivimusic/contributorsde.json` (materialized from the bundled default on first launch) so the list is editable without rebuilding; repo-root `contributorsde.json` remains the shipped default with a clearer multi-line `_guide`; vivizzz007 first in the default order.
- [x] DE 1.50.11 (PATCH): YouTube-style buffered progress on every player's seek bar (fainter secondary segment while the stream is still downloading, advancing even while paused; hidden once fully cached); seek bars can be scrubbed before playback starts (loaded-but-not-started tracks remember the position and play starts from it); About -> Contributors refreshed live from GitHub every time the screen opens (local copy kept as offline fallback).
- [x] DE 1.50.12 (PATCH): dedicated Contributors sub-screen (About -> Contributors) — About page keeps a single row under the lead developer and the list moved to its own screen; each entry shows the real name plus handle ("Name (@username)") when a `name` is set; `contributorsde.json` is always read live from the repository (silent refresh on open, stale ~/.vivimusic copy deleted, bundled copy only as offline fallback). Seek bars are usable even before the duration is known on a loaded-but-never-played track: the scrub stores a start fraction and playback begins from that point once the length is known.
- [x] DE 1.50.20 (PATCH): scrubbing the seek bar on a track whose stream was never resolved now starts the stream right away — the resolution/load is kicked off by the scrub itself and playback begins from the scrubbed position (YouTube behavior, issue #11), instead of only remembering the position until play is pressed; the YouTube-style buffered bar is now always visible (issue #12): the fainter secondary segment no longer vanishes once the download finishes or the track is cached — it stays full-width like a fully buffered YouTube video.
- [x] DE 1.50.19 (PATCH): uninstall now keeps exactly one final backup and wipes every cache (issue #10) — Windows Inno uninstaller copies device-sync.json, playlists.json and imported fonts into ~/.vivimusic/backups/uninstall-<timestamp>/ right before finishing, then deletes everything else in ~/.vivimusic (downloaded updates, audio/video/canvas/lyrics caches, logs, helper libraries, artwork), keeping only that last backup; Linux mirrors it via a .deb postrm hook (shared scripts/uninstall-cleanup.sh embedded in /opt, copied to /usr/share by postinst), AUR post_remove hook via vivi-music-de.install, and macOS/AppImage run the same script manually; INSTALL-GUIDE updated for every OS. Installer details box (issue #9) recreated at runtime with TNewMemo on both the Installing and uninstall pages (Inno Setup 6 removed the built-in DetailsMemo/DetailsButton, so the old Pascal did not compile).
- [x] DE 1.50.13 (PATCH): mini player seek bar fixed for tracks restored from the persistent queue (loaded but never played) — restored tracks had no duration, so the bar used a 0..1 range and the thumb snapped back after a scrub (the seek looked dead and required opening the full player); the scrubbed fraction is now kept visible on all seek bars (shown as a percentage until the real duration is known) and playback starts from it, and the real duration is written back into the persistent queue once a track is played so the next launch has the correct seek range immediately.
- [x] DE 1.50.14 (PATCH): sidebar Playlists header now navigates to the full playlist list screen (LocalPlaylistsScreen); only the chevron arrow expands/collapses the inline playlist list (issue #2).
- [x] DE 1.50.15 (PATCH): reduced audio micro-pauses/skips on macOS (issue #3) — output line buffer raised to ~250 ms of audio (from ~90 ms), atom scans bounded to 256 KB windows, frames queued before scanning, and seek slider/lyrics now follow the real playhead instead of the decode-ahead position. DE 1.50.14 (PATCH): sidebar Playlists header now navigates to the full playlist list screen (LocalPlaylistsScreen); only the chevron arrow expands/collapses the inline playlist list (issue #2). DE 1.50.13 (PATCH): mini player seek bar fixed for tracks restored from the persistent queue (loaded but never played) — restored tracks had no duration, so the bar used a 0..1 range and the thumb snapped back after a scrub; the scrubbed fraction is now kept visible on all seek bars and playback starts from it, and the real duration is written back into the persistent queue once a track is played.
- [x] DE 1.50.16 (PATCH): native system playback controls + "Now Playing" tile on macOS via a bundled MediaPlayer.framework helper (issue #4) — the current track is registered system-wide, physical media keys (keyboard/Touch Bar/headset) control the player without Accessibility permission, scrubbing from the Lock Screen slider seeks, and the tile shows title/artist/cover (artwork cached in ~/.vivimusic).

- [x] DE 1.50.17 (PATCH): Radio/Charts discovery screen fixed (issue #5 — chart parser now converts songs, video-chart playlists, top artists and albums instead of dropping every non-song type); native macOS notifications via UNUserNotificationCenter bundled in the media helper (issue #6); Home can no longer be silently blank — auto-retry + clear empty state with Retry (issue #7); changelog `#N` references are clickable GitHub links, past and future (issue #8).
- [x] DE 1.50.18 (PATCH): Inno Setup installer now always shows the installation details/log box below the progress bar on the Installing page (the "Show details" toggle is removed so it cannot be collapsed).

- [x] DE 1.50.40 (PATCH): the two "keep the queue going" settings ("Auto load more songs" and "Enable similar content") were duplicates gating the same code — merged into the single "Auto load more songs" toggle (issues #44); "Instantly skip silence" now only shows when "Skip silence" is on (issue #45); the slider style setting now applies to every player (classic mini player + expressive player seek/volume sliders, not only the classic full player) (issue #46).

- [x] DE 1.50.41 (PATCH): lyrics cache is now keyed by the fetch mode (synced-first vs first-answer, new v4 files) so a stale plain/wrong-version entry cached earlier can no longer hide the synced-first resolver fixes (issue #47); results fetched without a known duration are shown but not cached, so the search re-runs with a precise match once the duration is known; the album is passed to every lyrics provider like on mobile.

- [x] DE 1.50.42 (PATCH): Inno Setup uninstall crash fixed (try/except on TNewMemo during uninstall); metadata corrected (PiBOH author, "VIVI Music" display name, Linux Audio category); changelog versions 1.50.36-38 restored as own headings; website screenshot gallery JS fixed (duplicate script tag, DOMContentLoaded wrapper). (issues #48, #49, #50, #51)

- [x] DE 1.50.49 (PATCH): `BrowseScreen` crash fixed — `LazyVerticalGrid` keyed cards by `ytItem.id` alone, so a duplicate album/playlist/song appearing in two sections produced `Key "RDCLAK…" was already used`; keys are now scoped by `browse-$section-$position-$id`. New error code **E1031 `BROWSE_UNAUTHENTICATED`** added to `ERRORS.md` for `POST …/youtubei/v1/browse → 401 UNAUTHENTICATED` (New release albums, etc. — missing/expired `__Secure-3PAPISID`).

- [x] DE 1.50.51 (PATCH): browse 401 E1031 (FEmusic_new_releases_albums etc.) no longer fails — (a) partial auth (cookie without SAPISIDHASH) now falls back to anonymous instead of sending a rejected auth, (b) authed browse that hits 401 retries once anonymously so public catalog pages load even with an expired session (innertube browse retry + ytClient fallback).

- [x] DE 1.50.50 (PATCH): browse 401 E1031 failures now logged at browse time (logs/<ts>/browse.log with browseId/loggedIn/cookie context + prefixed E1031 in the error card) and every session now creates all 9 category logs even when empty — fixes the empty/missing-browse-log report (see vivi-de-logs-...ERROR E1031.zip).

- [x] DE 1.50.44 (PATCH): Home screen no longer crashes (NPE) when clicking any button — the Home LazyColumn content now bails out to an empty list while a chip/toggle reload nulls `home` instead of dereferencing it with `!!` in the snapshot observer (issue #54). Crash dumps are now written on every uncaught error to ~/.vivimusic/crash.log (always overwritten) and ~/.vivimusic/logs/<timestamp>/crash_<timestamp>.log (kept forever); every UI click is recorded in the session actions.log with a human-readable label (cards, song rows, section headers, mood chips, back buttons, Home controls, navigation) (issue #55).

- [x] DE 1.50.52 (PATCH): tracks no longer show a ~19 s duration (AudioPlayer derived the length from the first ~256 KB scan window and froze it; now the metadata duration is used as fallback and the AAC-derived length grows with the sample table). Listen Together: leaving a room forces a clean socket reconnect so create/join work again after a leave, and the lobby has a single morphing Create/Join room button like the mobile app.

- [x] DE 1.50.53 (PATCH): seek bar still stuck at ~19 s for some tracks — when the whole file was already on disk the sample table only held the first ~256 KB window, so complete cached tracks were misjudged as truncated (re-downloaded on every play) and truncated cache files played ~19 s and "ended". AudioPlayer now scans the entire file when it is already on disk (duration correct immediately, truncation guard only fires for real truncation). Also deduplicated `formatBytes`/`formatSpeed` (3+2 copies) and the 9 copy-pasted `Json { ignoreUnknownKeys = true }` codecs into shared `sharedJson*` values (cleaning).
